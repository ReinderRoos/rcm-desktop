# Issue 03 — Backwards compatibility + schema parity + cache safety

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice51-aging-distributies/PRD.md` (slice 51).

## What to build

Borg fase-1 compatibiliteit: bestaande projecten zonder nieuwe velden blijven hetzelfde rekenen, terwijl de nieuwe invoerschema's formeel onderdeel zijn van domain model + editing schema registry.

Deze slice maakt de migratieregel expliciet (`aging_distribution` ontbreekt => `normal`), houdt `failure_type` top-level intact, en zorgt dat cache-invoerhashes niet stiekem oude semantiek hergebruiken.

## Acceptance criteria

- [x] Deserialisatie van oudere projectbestanden zonder `aging_distribution` en zonder `beta_jaar` blijft succesvol en kiest impliciet `normal`.
- [x] `FailureType` blijft `random|aging`; `aging_distribution` is subkeuze onder aging en veroorzaakt geen breaking enum-migratie.
- [x] Editing schema registry en parity-tests zijn bijgewerkt voor nieuwe faalwijzevelden.
- [x] `CACHE_INPUTS_VERSION` is verhoogd vanwege gewijzigde motorinvoer.
- [x] Regressietests tonen dat fase-1 `normal`-gedrag niet verschuift op bestaande fixtures.

## Blocked by

- `.scratch/rcm-desktop-slice51-aging-distributies/issues/01.md`
