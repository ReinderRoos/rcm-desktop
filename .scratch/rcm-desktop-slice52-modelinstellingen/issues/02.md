# Issue 02 — Standaard faalparameters: `default_sigma_fraction` live

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice52-modelinstellingen/PRD.md` (slice 52).

## What to build

Breid Modelinstellingen uit met sectie **Standaard faalparameters** en maak **`default_sigma_fraction`** daadwerkelijk motor-relevant.

**Kern:**

- **`Faalwijze.effective_sigma`:** bij `sigma_jaar == 0` gebruik `project.config.default_sigma_fraction × mttf_jaar` (niet hardcoded 0,15). Pas aanroepers aan die effective sigma nodig hebben (property → methode met config-context of equivalent).
- **`CACHE_INPUTS_VERSION` bump** — gedrag wijzigt voor bestaande projecten met `sigma_jaar = 0`.
- Bestaande validator **`CFG_SIGMA_FRAC`** blijft gelden.

**Adapter + UI:**

- Dialoog-sectie **Standaard faalparameters:** `default_mttf_multiplier`, `default_sigma_fraction` met tooltips (multiplier beïnvloedt vooral PBS-MTTF-defaults, niet retroactief alle FM-MTTF’s).
- `commit_draft` markeert `requires_rerun` bij wijziging van deze velden.

**Tests / baselines:**

- Unit-test: FM met `sigma_jaar=0` geeft andere effective sigma na config-wijziging 0,15 → 0,20.
- Gerichte baseline-updates waar Haarlem (of andere fixtures) meetbare metric-shift tonen; geen blind portfolio-reset.

## Acceptance criteria

- [x] Motor en LCC-paden gebruiken config-gestuurde effective sigma voor FM’s met `sigma_jaar = 0`.
- [x] `CACHE_INPUTS_VERSION` is verhoogd; cache-tests of digest-tests blijven groen.
- [x] Dialoog toont en bewaart multiplier + sigma-fraction; wijziging zet `requires_rerun`.
- [x] Adapter + kern unit-tests; relevante regressietests groen (eventueel bijgewerkte asserts gedocumenteerd).

## Blocked by

- Issue 01 — `.scratch/rcm-desktop-slice52-modelinstellingen/issues/01.md` (dialoog + commit-pad moet bestaan)
