# Issue 03d — Adapter bundel-inzicht (scope, trekkers, jaarrijen)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice54-meekoppel-workflow-seam/PRD-preview-insight.md`

## What to build

Breid het preview-contract uit dat `MeekoppelWorkflowService` teruggeeft (Qt-vrij, test-first):

- `BundleScopeKind`: `location_row` | `pbs_selection` met scope-label en task-count.
- Per taak: `MeekoppelPreviewTaskRow` met baseline, effectief, doel, delta (doel − effectief), `is_target_driver`, blocked-reden.
- **Hybride default:** precies één locatietabelrij geselecteerd → rij-scope; anders hele PBS-selectie.
- **Trekkers:** taken waar effectief == berekend doeljaar; summary-regel “Bepaald door …” (één taak of “N taken op jaar X”).
- **Sorteer:** trekkers eerst, dan |delta|, dan taaklabel.
- **Scope-toggle API:** herbereken preview voor alternatieve scope; documenteer reset-semantiek voor checkbox-model (consumer: issues 03e/03f).

Kernvorm (besluit uit grill-me):

```python
BundleScopeKind = Literal["location_row", "pbs_selection"]
# MeekoppelTargetDriver, MeekoppelPreviewTaskRow op preview payload
```

HWP-IN-klasse scenario in unit tests: locatierij alle baseline/effectief gelijk → geen verschuiving; PBS-scope bevat taak met effectief 21 → doel + benoemde trekker(s).

## Acceptance criteria

- [x] Hybride default scope en expliciete scope-metadata op preview payload (adapter tests).
- [x] HWP-IN-scenario: rij-scope vs PBS-scope gedrag zoals PRD (adapter tests).
- [x] Task rows met baseline, effectief, doel, delta; delta = effectief → doel (adapter tests).
- [x] Trekkers, summary line, sorteer volgorde (adapter tests).
- [x] Scope-toggle API herberekent scope, drivers en rijen (adapter tests).

## Blocked by

- `.scratch/rcm-desktop-slice54-meekoppel-workflow-seam/issues/02.md`
- Slice **55** afgerond (parent-rollup + Haarlem GO) — zie `.scratch/rcm-desktop-slice55-meekoppel-discovery-parent-rollup/SLICE_MAP.md`
