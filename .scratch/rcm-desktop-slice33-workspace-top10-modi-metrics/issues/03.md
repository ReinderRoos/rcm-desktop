# Issue 03 — Niet-beschikbaarheid Top 10: lifecycle, per jaar (Ø/jaar), uren/%

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice33-workspace-top10-modi-metrics/PRD.md` (slice 33).

## What to build

Lever **Niet-beschikbaarheid** in Top 10 met horizon- en weergave-keuzes, end-to-end van bucket-math tot chart/tabel.

### Presentatiestate (sticky)

```python
@dataclass(frozen=True)
class ContributionPresentation:
    horizon: Literal["lifecycle", "per_year"] = "per_year"
    year_choice: Literal["average"] | int = "average"  # int = kalenderjaar
    unavailability_display: Literal["hours", "percent"] = "hours"
```

Alleen actief wanneer metric = `niet_beschikbaarheid`.

### Deep module (Qt-vrij): contribution horizon values

Per `FMResult` in scope een scalar voor aggregatie:

| Modus | Uren | % |
|-------|------|---|
| Per LCC-periode | lifecycle downtime totaal (CM+hidden+PM, reconcile motor) | bestaande lifecycle % (downtime / lifecycle-uren × 100) |
| Per jaar, Ø | gemiddelde bucket-uren | gemiddelde bucket-% of uren/8760×100 |
| Per jaar, jaar K | bucket K uren | bucket K uren / 8760 × 100 |

Bucket-SSOT: zelfde patroon als bestaande NB-proxy (`horizon_profile`, legacy fallback + reconcile naar lifecycle downtime).

### UI (sub-balk)

- Segmented **Per LCC-periode | Per jaar** (zichtbaar bij metric NB).
- Combobox **Ø per jaar | kalenderjaren** (alleen bij *per jaar*).
- Segmented **Uren | %** (alleen bij metric NB; default **Uren**).

### Integratie

- `build_contribution_rows` gebruikt horizon-service i.p.v. vaste NB%-extractor.
- Table formatting: uren → `h`; % → `%` (4 decimalen pariteit).
- Render/cache-keys nemen `ContributionPresentation` op (minimaal in workspace render signature).

## Acceptance criteria

- [x] Adapter unit: lifecycle NB% matcht huidig gedrag binnen tolerantie; lifecycle uren = som FM downtime (scope).
- [x] Adapter unit: som bucket-uren over horizon ≈ lifecycle totaal (1e-3); Ø per jaar = som buckets / N.
- [x] Adapter unit: specifiek kalenderjaar K = bucket K; % = uren_K / 8760 × 100.
- [x] Top 10 met metric NB: toggles wijzigen ranking zichtbaar; Kosten-metric verbergt horizon/NB-weergave-toggles.
- [x] Default bij NB: per jaar + Ø per jaar + Uren.
- [x] pytest-qt: sub-balk toont jaar-combobox alleen bij *per jaar*; Uren|% alleen bij NB.
- [x] Geen `rcm_core`-schema-wijziging.

## Blocked by

- Issue 02 — `.scratch/rcm-desktop-slice33-workspace-top10-modi-metrics/issues/02.md`
