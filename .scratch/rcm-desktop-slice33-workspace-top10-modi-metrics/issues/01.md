# Issue 01 — Drie modi: NB/PM-modus volledig weg

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice33-workspace-top10-modi-metrics/PRD.md` (slice 33).

## What to build

Vereenvoudig de resultatenwerkruimte naar **drie modi**: **Top 10**, **Tijdsplot**, **FM-detail**.

End-to-end:

- Verwijder modus-knoppen *Niet-beschikbaarheid* en *Preventief onderhoud* uit de toolbar.
- Verwijder bijbehorende detailpagina’s, chart/table-widgets en render-paden uit de werkruimte-view (geen imports/renders meer naar NB/PM-modus).
- Pas `ResultsWorkspaceState` aan: alleen `bijdragen`, `lcc`, `fm_detail` in `ALL_MODES`; verwijder `pm_submode` en setters uit snapshot (of laat migratie negeren).
- **Migratie** bij `set_modus` / snapshot-normalisatie:
  - `preventief_onderhoud` → `lcc`
  - `niet_beschikbaarheid` → `bijdragen`
- Werk bestaande tests bij die vijf modi, PM-submodus of NB/PM-pagina’s verwachten; Tijdsplot- en Top 10-smoke blijven groen.

**Nog niet in deze slice:** Top 10-sub-balk, horizon-toggles, metric-wijzigingen (issue 02+).

## Acceptance criteria

- [x] Toolbar toont precies drie modus-knoppen: Top 10, Tijdsplot, FM-detail.
- [x] Geen route meer naar NB- of PM-detailpagina; moduswissel crasht niet.
- [x] State met oude modus-string wordt genormaliseerd naar `lcc` of `bijdragen` zonder exception.
- [x] pytest-qt: open werkruimte na run → alleen drie modus-knoppen zichtbaar; Tijdsplot en Top 10 blijven bereikbaar.
- [x] Geen `rcm_core`-wijziging; geen directe kern-imports in views (typing-only blijft).

## Blocked by

None — can start immediately.
