# Issue 01 — Run-pad CM-overlay default materialisatie (C1)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice68-parity-run-alignment/PRD.md` (slice 68, issue 01).

## What to build

Sluit de gap tussen slice 67 unit tests en de desktop run-keten: **standaard run** moet dezelfde PM-set gebruiken als counterfactual `ef_cm_overlay` wanneer `import_settings.aw_disabled_pm_ids` uit AW-import komt.

Centraliseer in `run_service`: materialiseer disabled PM's vóór `run_incremental_analysis`. What-if `PlanningOverlayState` blijft leidend wanneer actief.

Validate-venster moet hetzelfde gedrag krijgen (via run_service-default, niet alleen werkruimte overlay-seed).

## Acceptance criteria

- [ ] `run_service.run(Gaarkeuken)` zonder expliciete overlay: disabled PM's niet in run-project.
- [ ] `build_failure_validation_report` na run: median `|Δ scenario| < 0.01` (135 FM's).
- [ ] What-if overlay met aangepaste `disabled_pm_ids` overschrijft import-default.
- [ ] `test_desktop_run_service_materializes_overlay_disabled_pm` blijft groen; uitbreiding met import-seed scenario.
- [ ] Cache invalidatie wanneer overlay/disabled-set wijzigt (geen stale ef_actual).

## Blocked by

None
