# Issue 02 — EffectIds parser + import-audit

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice69-ohs-effect-import-alignment/PRD.md` (slice 69, issue 02).

## What to build

Implementeer parsing van `RcmCauses.EffectIds` (komma-gescheiden, optioneel `[RF=0,1]` / `[RF=0.5]`).

Cross-check geparsede effecten + RF tegen FM-links uit assignments per cause. Mismatch → importwaarschuwing. Optioneel: `import_settings.isograph_causes[fm_id].effect_ids_parsed`.

**Geen** link-creatie vanuit EffectIds — alleen audit.

## Acceptance criteria

- [ ] Unit tests: meerdere effecten, NL decimaal in RF-suffix, lege EffectIds.
- [ ] Synthetische import: mismatch EffectIds vs assignments → waarschuwing in `result.warnings`.
- [ ] Voorbeeld-cause: parsed EffectIds consistent met 4 FM-links (geen false mismatch).

## Blocked by

Issue 01 (semantiek vast)
