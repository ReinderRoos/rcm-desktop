# Issue 05 — PM dual-scope (PEnable + IEnable)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice69-ohs-effect-import-alignment/PRD.md` (slice 69, issue 05).

## What to build

Vervang single-task `resolve_pm_task_id` door per-scope resolutie: wanneer `PEnable` true → Planned-scope tasks; wanneer `IEnable` true → Inspection-scope tasks. Meerdere `PMEffectLink` per assignment-rij mogelijk.

Behoud unieke `link_id` per `pm_id` + effect.

## Acceptance criteria

- [ ] Synthetische test: P+I true, REV + IN op verschillende SubIndex → twee PM-links voorzelfde effect.
- [ ] P-only en I-only blijven werken (bestaande tests groen).
- [ ] Geen dubbele link voor dezelfde `pm_id` + effect.

## Blocked by

Issue 04
