# Issue 04 — PM-resolutie SubIndex-fallback

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice69-ohs-effect-import-alignment/PRD.md` (slice 69, issue 04).

## What to build

Breid `isograph_pm_import_rules` uit: wanneer geen ScheduledTask op assignment-SubIndex, fallback naar **unieke** task in gevraagde scope (Planned of Inspection) op dezelfde cause.

Emit waarschuwing `SubIndex-fallback` met cause/effect/sub. Bij 0 of >1 kandidaten: geen fallback (bestaand gedrag).

Integreer in `_build_effect_links`.

## Acceptance criteria

- [ ] Synthetische test: assignment SubIndex 2, alleen REV SubIndex 0 → PM-link + fallback-waarschuwing.
- [ ] Ambiguous meerdere REV's zonder SubIndex-match → geen link + ambiguous/unresolved waarschuwing.
- [ ] Voorbeeld-cause: PM-link voor `Schutten 0-20%` waar REV SubIndex 0 bestaat (indien spike dat toelaat).

## Blocked by

Issue 01
