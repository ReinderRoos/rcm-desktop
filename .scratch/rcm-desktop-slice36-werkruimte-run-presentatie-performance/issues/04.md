# Issue 04 — Modus-gescopeerde rerender (slice 25 gap)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice36-werkruimte-run-presentatie-performance/PRD.md` (slice 36).

## What to build

Maak **`workspace_detail_split_render_depth`** effectief: `_rerender_detail_for_current_scope` **gebruikt** `split_render_depth` i.p.v. `del split_render_depth`.

Introduceer een **pure beslisfunctie** `required_detail_builders(snapshot, depth) -> ...` die bepaalt welke modus-adapters (FM-detail, Bijdragen, LCC) in een tick herbouwd moeten worden. Bij **`active_modus_only`**: geen LCC/LTAP-build wanneer gebruiker in FM-detail of Top 10 zit en alleen presentatie-toggles wijzigen die die modus niet raken.

KPI-refresh: scope-wijziging blijft volledige refresh; pure metric/filter-wissel binnen Top 10 minimaliseert werk waar veilig.

## Acceptance criteria

- [ ] Unit tests op pure beslisfunctie: scope-change → `all_splits`; metric-only toggle in Bijdragen → geen LCC-builder requirement.
- [ ] pytest-qt: moduswissel FM ↔ Bijdragen triggert juiste panels; geen regressie op PBS-scope filtering.
- [ ] `del split_render_depth` anti-pattern verwijderd; parameter wordt daadwerkelijk gebruikt.
- [ ] Bestaande workspace state tests groen.

## Blocked by

None — can start immediately.

## Notes

- Slice 25 issue 03 was **done** maar implementatie incompleet — dit issue sluit die gap expliciet.
