# Issue 05 — Motor: horizon_profile bij run

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice36-werkruimte-run-presentatie-performance/PRD.md` (slice 36).

## What to build

Koppel **slice 27 jaartoerekening** aan de run-pipeline: elk `FMResult` uit `run_analytical` krijgt een **`horizon_profile`** vóór cache-persist. Presentatie (LCC, niet-beschikbaarheid, FM-inspector) leest profiel; **legacy `_legacy_*` bucket-paden** alleen als fallback voor oude cache zonder profiel.

Verhoog **`CACHE_INPUTS_VERSION`** in de kern wanneer cache-trust of resultaat-vorm dat vereist. Documenteer in issue-outcome.

## Acceptance criteria

- [ ] Na run op test-fixture: FM-results in cache bevatten `horizon_profile` met bucket-lengte consistent met lifecycle.
- [ ] `build_cm_eur_per_bucket` / NB-services gebruiken profiel zonder `used_legacy=True` op verse runs (testbaar flag/assert).
- [ ] FM-inspector toont jaartabel op fixture-run (geen `profile_missing` op verse data).
- [ ] `tests/test_editing_schemas_parity.py` en incrementele run tests groen.
- [ ] `CACHE_INPUTS_VERSION` bump indien nodig + korte notitie in commit/handoff.

## Blocked by

- Issue 01 (aanbevolen: run-pad stabiel vóór motor-output wijzigt; kan parallel met voorzichtigheid)

## Notes

- Slice 34 issue 04 bewust **zonder motor** — dit issue is de bewuste motor-koppeling.
- Geen wijziging aan scrub-list of import-semantiek.
