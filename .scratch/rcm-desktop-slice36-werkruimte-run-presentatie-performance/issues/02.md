# Issue 02 — LTAP presentatie-cache (memoization)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice36-werkruimte-run-presentatie-performance/PRD.md` (slice 36).

## What to build

**Diepe Qt-vrije module** die `build_ltap_view` resultaten **memoized** per canonieke sleutel: overlay-state (disabled PM-ids + anchor years), PBS-scope (`fm_pbs_ids`), optionele taaktype-filter. **Invalidate** bij project-wissel of overlay-mutatie.

Integreer in alle hot paths die LTAP herhaald aanroepen: **LCC-planning**, **presentatie-cache build** (LCC/PM/NB), zonder gedrag te wijzigen — alleen dubbele CPU elimineren.

## Acceptance criteria

- [ ] Twee opeenvolgende calls met identieke kwargs retourneren hetzelfde `LTAPView`-object (identity) of gelijkwaardige immutable snapshot — vastgelegd in unit test.
- [ ] Overlay-wijziging (anchor/disabled set) produceert cache-miss en nieuwe view.
- [ ] Project-wissel wist cache volledig.
- [ ] Stub/counter-test: `build_lcc_planning_curve` met vaste inputs roept onderliggende LTAP-berekening **maximaal 2×** (baseline + overlay), niet meer door proxy-lagen.
- [ ] Geen Qt-imports in cache-module; TDD op adapterlaag.

## Blocked by

None — can start immediately (synergie met issue 03).

## Notes

- Cache-key moet deterministisch zijn (gesorteerde ids, genormaliseerde overlay-dict).
