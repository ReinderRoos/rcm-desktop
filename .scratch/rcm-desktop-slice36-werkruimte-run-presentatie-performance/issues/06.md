# Issue 06 — Performance-regressietests (call-count + baseline)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice36-werkruimte-run-presentatie-performance/PRD.md` (slice 36).

## What to build

Uitbreiding `tests/perf/` met **niet-flaky** performance-guardrails voor slice 36:

- **Cache-hit contract:** tweede incrementele run op ongewijzigd project → `recalculated_fm_count == 0`.
- **LTAP call-budget:** LCC-planningcurve ≤ 2 LTAP-view builds; chart + jaardetail cyclus hergebruikt cache (≤ 2 unieke builds).
- **LCC jaardetail:** geen curve-rebuild wanneer `planning_curve` meegegeven.
- **Render-scoping:** metric-only toggle in Bijdragen vereist geen LCC-builder.
- **Baseline:** contract-drempels in `tests/perf/baseline.json`; handmatige Haarlem-timing blijft gedocumenteerd.

Tests krijgen `@pytest.mark.perf` (standaard uitgesloten via `addopts`).

## Acceptance criteria

- [ ] `tests/perf/test_slice36_regression.py` met bovenstaande contracten.
- [ ] `baseline.json` uitgebreid met `slice36_contracts` (geen wijziging aan handmatige Haarlem-seconden).
- [ ] `tests/perf/README.md` beschrijft run-instructie (`pytest -m perf`).
- [ ] `pytest -m perf tests/perf/test_slice36_regression.py` groen lokaal.

## Blocked by

- Issue 01
- Issue 02
- Issue 03
- Issue 04

## Notes

- Geen timing-asserties in CI (flaky); alleen call-count / cache-only gedrag.
