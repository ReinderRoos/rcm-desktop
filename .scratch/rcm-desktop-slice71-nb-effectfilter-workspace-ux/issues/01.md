# Issue 01 — EffectNbFilterSet + NB-seam EffectImpactService

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice71-nb-effectfilter-workspace-ux/PRD.md` (slice 71, issue 01).

## What to build

Qt-vrije uitbreiding deep module `EffectImpactService`:

- `EffectNbFilterSet` dataclass (`selected_klasse_ids: frozenset[str]`; leeg = alle NB totaal).
- `list_nb_effect_klassen(project)` → alleen effectklassen met categorie beschikbaarheid.
- `nb_scalar_for_fm(project, fmr, *, klasse_ids, presentation)` → NB uren of % per FM.
- `nb_yearly_series(...)` basis (multi-klasse som) — volledige Tijdsplot in issue 05.

Semantiek lege filter: totale NB (slice 33: CM + PM + verborgen NB), **niet** som enkel effectlinks.

## Acceptance criteria

- [ ] Unit tests: lege filter ≈ bestaand totaal-NB scalar voor golden cause FM.
- [ ] Unit tests: filter op één Schutten-klasse = `aggregate` NB voor die klasse.
- [ ] VGM-klassen niet in `list_nb_effect_klassen`.
- [ ] Geen Qt-imports.

## Blocked by

Issue 00
