# Issue 03 — Workspace state + verwijder hybrid Effectklasse/Effectimpact

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice71-nb-effectfilter-workspace-ux/PRD.md` (slice 71, issue 03).

## What to build

- `WorkspaceStateSnapshot.effect_nb_filter: EffectNbFilterSet`.
- `set_effect_nb_filter(...)` op `ResultsWorkspaceState`.
- Verwijder `SOURCE_EFFECTKLASSE`, `METRIC_EFFECTIMPACT`, hybride `set_source`-logica.
- `normalize_modus` / legacy migratie voor oude source/metric strings.
- `build_workspace_cache_modus_key` + presentation cache: filter serialisatie.
- Orchestrator: `effect_nb_filter_visible` wanneer `metric == niet_beschikbaarheid` (Top 10 + Tijdsplot plans).

## Acceptance criteria

- [ ] Unit tests: snapshot bevat filter; subscribe emit bij wijziging.
- [ ] Legacy source `effectklasse` → normalize naar PBS + NB + lege filter.
- [ ] `ALL_SOURCES` heeft alleen PBS + Faalwijze.
- [ ] `ALL_METRICS` heeft drie entries.

## Blocked by

Issue 02
