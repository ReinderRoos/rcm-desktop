## Parent

PARENT_ISSUE_URL

## What to build

Qt-vrije uitbreiding `EffectImpactService`:

- `EffectNbFilterSet` (`selected_klasse_ids`; leeg = totale NB slice 33).
- `list_nb_effect_klassen(project)` — alleen categorie beschikbaarheid.
- `nb_scalar_for_fm(...)` — NB uren/% per FM.
- `nb_yearly_series(...)` — multi-klasse som (basis voor issue 05).

## Acceptance criteria

- [ ] Lege filter ≈ totaal-NB scalar golden cause.
- [ ] Enkele Schutten-klasse filter = aggregate NB die klasse.
- [ ] VGM niet in list_nb_effect_klassen.
- [ ] Geen Qt-imports.

## Blocked by

CHILD_00_ISSUE_URL
