# Issue 02 — Geen dubbel werk legacy single-slot vs scenario-split

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice25-results-workspace-ui-performance/PRD.md` (slice 25).

## What to build

In **scenario-modus**, wanneer de **gesplitste CM/PM-weergave** de gebruiker-
zichtbare waarheid is voor LCC, Bijdragen, Niet-beschikbaarheid en Preventief
onderhoud, **schrap of sla over** het vullen van de **legacy single-slot**
widgets die alleen voor backward-compat werden gevuld, **tenzij** een specifieke
modus ze nog nodig heeft (expliciet documenteren in code + tests).

## Acceptance

- Voor ten minste één fixture met **beide** scenario-slots gevuld: teller op
  `build_single_run_lcc_input` / `build_contribution_rows` (of equivalente
  service-entrypoints) daalt **meetbaar** t.o.v. main voor dezelfde UI-actie
  (modus open + scenario-switch), zonder visuele regressie in split-pane.
- FM-detail-gedrag in scenario-modus blijft correct volgens slice 23-contract.

## Notes

- Raakt orchestratie in de **resultatenwerkruimte**-vensterlaag; kern blijft ongewijzigd.
