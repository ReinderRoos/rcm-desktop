# Issue 01 — REV-segment survival: kernfix + unit-regressie

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice66-rev-segment-survival-fix/PRD.md` (slice 66).

## What to build

Corrigeer `_conditional_failures_with_rev_segments` in de distributions-laag zodat de conditionele noemer (`survival`) **per segment** wordt herberekend op de effectieve leeftijd na eerdere REV-rejuvenaties — niet één globale waarde vanaf studiestart.

Voeg een gerichte regressietest toe voor het Gaarkeuken-ankerscenario (oude asset, REV elke 20 jr, normaal aging).

Bump `CACHE_INPUTS_VERSION` na motorwijziging.

## Acceptance criteria

- [ ] `_conditional_failures_with_rev_segments` herberekent `survival` aan het begin van elk REV-segment voor `normal`, `truncated_normal_0`, en `weibull_2p`.
- [ ] Ankerscenario (leeftijd=44, mttf=25, σ=3,75, REV@20): `rem_clock=70` → `expected_failures` plausibel (~2, tolerantie in test); `rem_clock=90` → **niet** ~450.000 (assert bovengrens, bijv. `< 50`).
- [ ] Zonder `rev_schedule` gedrag ongewijzigd t.o.v. bestaande tests.
- [ ] `CACHE_INPUTS_VERSION` verhoogd.
- [ ] Bestaande `test_aging_monte_carlo` en distributions-gerelateerde tests groen.

## Blocked by

None — can start immediately
