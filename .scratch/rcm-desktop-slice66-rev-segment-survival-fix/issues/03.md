# Issue 03 — Documentatie: causaliteit bug ↔ AW MC-horizon

**Triage:** ready-for-agent  
**Type:** docs (licht)

## Parent

PRD: `.scratch/rcm-desktop-slice66-rev-segment-survival-fix/PRD.md` (slice 66).

## What to build

Voeg een korte `KANBAN_HANDOFF.md` toe onder deze slice-map met:

- samenvatting root cause en fix;
- baseline vóór fix (128/135 fails op Gaarkeuken);
- bevestiging dat `aw_mc_lifecycle_horizon` default `False` blijft;
- verwijzing naar ADR-0008 en slice 65.

Optioneel: één zin in ADR-0008 **References** of **Consequences** over REV-segment survival (alleen als projectconventie dat toelaat zonder ADR-heropening).

## Acceptance criteria

- [ ] `KANBAN_HANDOFF.md` aanwezig met reproduceerbare check (`build_parity_report` op Gaarkeuken).
- [ ] Geen wijziging aan default `aw_mc_lifecycle_horizon` in code.
- [ ] Geen UI-wijzigingen.

## Blocked by

Issue 02
