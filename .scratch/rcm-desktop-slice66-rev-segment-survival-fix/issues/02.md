# Issue 02 — Gaarkeuken parity-gate regressie

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice66-rev-segment-survival-fix/PRD.md` (slice 66).

## What to build

Voeg een regressietest toe die `run_incremental_analysis` (of `run_analytical`) + `build_parity_report` draait op `tests/fixtures/RCMCostdata export_Gaarkeuken.rcm.json` met het project zoals geladen (inclusief `aw_mc_lifecycle_horizon=True` indien in fixture).

Assert dat het aantal parity-**fails** sterk daalt t.o.v. de bekende baseline (**128 fails** vóór fix). Stel een conservatieve bovengrens (bijv. `fail_count ≤ 20`) — finetune na eerste groene run als AW-band restverschillen laat.

## Acceptance criteria

- [ ] Test gebruikt Gaarkeuken-fixture met `@pytest.mark.skipif` als bestand ontbreekt (zelfde patroon als `test_failure_parity_validation.py`).
- [ ] Test rapporteert `pass`, `fail`, `informative`, `missing` counts uit `build_parity_report`.
- [ ] `fail_count ≤ 20` (of strenger als eerste run dat toelaat — documenteer gekozen drempel in testcomment).
- [ ] FM `06H-350.2.12.2.1.A.1` parity-status is **pass** of informatief (niet fail met ×10⁵ kostenfactor).

## Blocked by

Issue 01
