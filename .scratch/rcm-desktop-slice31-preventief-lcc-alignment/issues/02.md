# Issue 02 — Werkruimte: Preventief-modus + what-if-hint

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice31-preventief-lcc-alignment/PRD.md` (slice 31).

## What to build

Koppel de resultatenwerkruimte aan issue 01 voor submodus **Kosten**:

- `_render_pm_for_snapshot` (en presentatie-cache waar van toepassing) gebruikt de
  gedeelde preventief-bron i.p.v. losse `build_pm_chart_input`-LTAP voor Kosten
  wanneer overlay actief of wanneer filters afwijken van “alles aan”.
- **Render-cache key** voor Preventief-modus bevat `pm_submode`, `planning_overlay`
  (active + change fingerprint) en `lcc_filters`.
- Toon vaste **statusregel** boven de sub-toggle wanneer what-if actief:
  *Kosten: what-if-preview · Aantal uitvoeringen: laatste analyse (baseline)*
  (NL-strings in `messages.py`).
- Submodus **Aantal uitvoeringen** blijft in deze issue ongewijzigd (issue 03).

## Acceptance criteria

- [x] Pytest-qt: project laden → analyse → what-if aan → REV passief → modus
      Preventief → Kosten: staaf lager dan vóór passief (zelfde orde als LCC).
- [x] Pytest-qt: what-if actief → hint zichtbaar; what-if uit → hint weg.
- [x] Pytest-qt: filter alleen REV in LCC → switch naar Preventief/Kosten → zelfde
      jaar-totalen als LCC preventief-segment (binnen 1e-3).
- [x] Geen regressie: default flow Preventief/Kosten zonder what-if blijft groen.
- [x] Geen directe `rcm_core`-imports in views (behalve typing-only).

## Blocked by

- `01.md`
