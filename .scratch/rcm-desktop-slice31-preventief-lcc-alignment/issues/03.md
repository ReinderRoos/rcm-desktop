# Issue 03 — Aantal uitvoeringen + taaktype-filters

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice31-preventief-lcc-alignment/PRD.md` (slice 31).

## What to build

Breid submodus **Aantal uitvoeringen** uit zodat LTAP-tellingen per kalenderjaar
dezelfde **taaktype-filters** (`lcc_filters`) respecteren als LCC en Kosten:

- Bron blijft `build_ltap_view` op **`last_run`** (geen overlay op counts).
- Filter op PM-taaktype (REV/IN/…) consistent met workspace-state.
- Presentatie-cache en render-key meenemen filters.

## Acceptance criteria

- [x] Adapter-test: filter alleen REV → som counts = REV-uitvoeringen in LTAP-view
      (zelfde scope); filter uit → totaal = `total_task_count` projectbreed.
- [x] Adapter-test: scoped subtree + filter → som = gefilterde subtree-counts.
- [x] Pytest-qt: what-if actief → Aantal wijzigt niet bij alleen passief zonder
      herberekenen; hint uit issue 02 blijft correct (“baseline”).
- [x] Geen wijziging in `rcm_core`.

## Blocked by

- `01.md`
