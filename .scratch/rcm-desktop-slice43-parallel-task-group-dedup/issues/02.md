# Issue 02 — Post-pass taakgroep-PM-deduplicatie (GREEN)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice43-parallel-task-group-dedup/PRD.md` (slice 43).

## What to build

Implementeer **I1** in de rekenkern: na de parallel `ProcessPoolExecutor`-pool een sequentiële **post-pass** met project-brede `counted_group_ids`, identieke FM-volgorde als het sequentiële pad.

**Deep module (nieuw):**

```python
def deduplicate_parallel_fm_pm_costs(
    project: RCMProject,
    fm_results: dict[str, FMResult],
    *,
    fm_ids: list[str] | None = None,
) -> dict[str, FMResult]:
    """Herbereken PM-velden met project-brede taakgroepdeduplicatie."""
```

- Per FM in vaste volgorde: `compute_pm_totals` met gedeelde `counted_group_ids` (SSOT, ongewijzigde semantiek).
- Werk bij: `pm_cost_eur`, `expected_pm_downtime_hr`, `effect_bijdragen` (FM-deel behouden + PM opnieuw), `total_cost_eur = expected_cm_cost_eur + pm_cost_eur`.
- **Niet** wijzigen: CM, faalmomenten, `horizon_profile`, `risk_contribution`.

**Integratie:**

- `compute_all_fm_results`: roep post-pass alleen aan wanneer parallel pool daadwerkelijk draaide (niet bij sequentieel, niet bij parallel-overgeslagen <8 FM’s).

Issue 01 parity-tests moeten **groen** worden.

## Acceptance criteria

- [ ] `deduplicate_parallel_fm_pm_costs` bestaat en is unit-testbaar (optionele directe unit tests).
- [ ] Parallel `run_analytical` ≡ sequentieel op mini-fixture en Haarlem (issue 01 tests groen).
- [ ] Sequentieel pad ongewijzigd: sequentiële Haarlem-baseline-tests blijven groen zonder baseline-aanpassing.
- [ ] Slice-42 LCC-characterisatie (H3) en slice-24 scenario-parallel tests blijven groen.
- [ ] `effect_bijdragen` consistent na post-pass (geen half-bijgewerkte FM/PM-mix).
- [ ] Geen Qt, geen adapter/run-policy wijzigingen.

## Blocked by

- Issue 01 — `.scratch/rcm-desktop-slice43-parallel-task-group-dedup/issues/01.md`
