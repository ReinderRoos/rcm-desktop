# Issue 01 — K1-paritytests parallel ≡ sequentieel (RED)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice43-parallel-task-group-dedup/PRD.md` (slice 43).

## What to build

Leg **K1-parity** vast vóór de motorfix: `run_analytical(project, parallel=True)` moet per faalwijze dezelfde waarden geven als `parallel=False` voor `total_cost_eur`, `pm_cost_eur` en `expected_pm_downtime_hr`.

**Minimale synthetische fixture (inline of klein JSON):**

- Kern: **2 faalwijzen** delen **1 `TaskGroup`** (beide met PM-taak in die groep) — maakt dubbeltelling zichtbaar.
- **≥8 faalwijzen** in totaal (bijv. 6 extra FM’s zonder PM of met eigen groep), want `compute_all_fm_results` gebruikt pas `ProcessPoolExecutor` bij `parallel=True` en `len(fms_to_calc) >= 8` (anders valt het terug op sequentieel en triggert de bug niet).

**Haarlem-demo:**

- Parity op `tests/fixtures/awzi_haarlem_waarderpolder_demo.rcm.json` (121 FM, parallel pool actief).

**Vervang inflatie-regressie:**

- Verwijder of vervang `test_parallel_fm_pass_inflates_task_group_costs` — geen test die dubbeltelling **verwacht**.

**Strikte toleranties (K1):**

- Per FM: `pytest.approx(rel=1e-6)` of `abs=1e-4` EUR waar passend voor de drie velden.

Geen wijziging aan `rcm_core/engine.py`, geen `CACHE_INPUTS_VERSION` bump in deze issue (issue 03).

## Acceptance criteria

- [ ] Nieuwe testmodule (of uitbreiding baseline-tests) met mini-fixture parity parallel ≡ sequentieel.
- [ ] Haarlem parity-test: alle FM’s, drie K1-velden, strikte toleranties.
- [ ] `test_parallel_fm_pass_inflates_task_group_costs` verwijderd of vervangen door parity (geen inflatie-as-expected).
- [ ] Tests zijn **RED** vóór motorfix (falen op dubbele taakgroep-PM) of expliciet `@pytest.mark.xfail` alleen als team TDD strikt issue 02 koppelt — voorkeur: harde RED zonder xfail.
- [ ] Sequentieel Haarlem-baseline (`test_haarlem_run_metrics_match_*`) nog niet gewijzigd.
- [ ] Geen wijzigingen aan `rcm_core.models`, editing schemas, adapter of views.

## Blocked by

None — can start immediately.
