# Issue 03 — Cache bump, docstring, handoff

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice43-parallel-task-group-dedup/PRD.md` (slice 43).

## What to build

Sluit slice 43 af nadat motor + parity groen zijn:

1. **`CACHE_INPUTS_VERSION`:** 103 → **104** (invalidateert FM-cache met foute parallel-PM).
2. **Docstring** in `compute_all_fm_results`: corrigeer misleiding dat `compute_pbs_results` taakgroepen dedupliceert.
3. **`KANBAN_HANDOFF.md`** in slice-43 scratch-map: besluiten (F1/I1/S1/K1/T1), testcommando’s, verwachting dat scenario-totalen kunnen dalen na Herbereken, verwijzing naar uitgestelde S2 (desktop Start analyse parallel).

Optioneel: assert in bestaande cache-test dat versie 104.

Geen parallel inschakelen in desktop Start analyse (S2 blijft apart).

## Acceptance criteria

- [ ] `CACHE_INPUTS_VERSION == 104`; bestaande cache-version tests bijgewerkt indien aanwezig.
- [ ] Docstring reflecteert post-pass deduplicatie (niet PBS-aggregatie).
- [ ] `KANBAN_HANDOFF.md` aanwezig met testcommando’s en kanban-vervolg (S2, D3).
- [ ] Volledige slice-43 pytest-set groen (issues 01–02).
- [ ] Geen wijzigingen aan models/editing schemas/views.

## Blocked by

- Issue 02 — `.scratch/rcm-desktop-slice43-parallel-task-group-dedup/issues/02.md`
