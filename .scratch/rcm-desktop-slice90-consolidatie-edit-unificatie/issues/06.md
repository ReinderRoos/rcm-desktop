# Issue 06 — Validate-venster dirty via EditingHost

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice90-consolidatie-edit-unificatie/PRD.md` (slice 90).

## What to build

**Tracer bullet** voor edit-unificatie (werkstroom B): het Validate-venster
gebruikt dezelfde buffer-brede dirty-seam als de nette afsluitprocedure — geen
eigen digest-checks meer op faalwijzen.

End-to-end:

- Alle `_faalwijzen_edit.is_dirty()`-checks in het Validate-venster (opslaan,
  sluiten, run-start, dirty-guards) vervangen door `EditingHost.is_grid_dirty()`
  op `edit_dirty_global`.
- Opslaan/verwerpen in Validate-venster aligneren met slice 89-semantiek
  (buffer-breed via host-seam en dirty-guard-policy waar van toepassing).
- Geen zichtbaar gedrag voor de analist behalve consistentie met afsluitprocedure
  (wijziging in niet-faalwijzen-tabellen wordt nu ook in Validate gedetecteerd).
- Tests op extern gedrag: dirty-guards in Validate-venster-scenario's (prior art:
  slice 89 dirty-seam-tests).

## Acceptance criteria

- [ ] Geen `is_dirty()`-calls meer op faalwijzen-/entity-edit-service in
      Validate-venster voor buffer-dirty.
- [ ] Wijziging in een niet-faalwijzen-entiteit triggert dirty-guard in Validate.
- [ ] Schone buffer na commit/verwerpen in Validate; `is_grid_dirty()` is `False`.
- [ ] Bestaande Validate-UX-tests blijven groen of zijn aangepast aan buffer-breed
      gedrag.
- [ ] Zelfde dirty-semantiek als afsluitprocedure (slice 89).

## Blocked by

- Issue 05 (`.scratch/rcm-desktop-slice90-consolidatie-edit-unificatie/issues/05.md`)
  — digest moet weg zijn; host-seam is enige bron.

## User stories covered

10, 11, 18
