# Issue 02 — Tracker-sync en .gitignore voor gegenereerde bestanden

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice90-consolidatie-edit-unificatie/PRD.md` (slice 90).

## What to build

**Tracer bullet** voor operationele consolidatie (werkstroom A): de lokale
issue-tracker weerspiegelt de werkelijke staat van de code en gegenereerde
cache-/bak-bestanden vervuilen de git-status niet meer.

End-to-end:

- Slices 87–89 (PRD's en issues) markeren als afgerond: triage-labels naar
  `done` (of equivalent in dit project) waar code en tests al gecommit zijn.
- `.gitignore` uitbreiden voor patronen zoals `*.cache.*.json`, `*.rcm.json.bak`
  en vergelijkbare gegenereerde artefacten uit tests/fixtures en perf-runs.
- Geen wijziging aan bestaande functionaliteit; alleen tracker- en
  repo-hygiëne.

## Acceptance criteria

- [ ] PRD's/triage voor slices 87–89 staan op done (niet meer `ready-for-agent`).
- [ ] Gegenereerde cache- en bak-bestanden verschijnen niet meer als untracked
      in een schone `git status` na een typische dev-sessie.
- [ ] `.gitignore`-patronen zijn specifiek genoeg om echte fixtures niet te
      verbergen.
- [ ] Geen bestaande getrackte bronbestanden worden per ongeluk genegeerd.

## Blocked by

None — can start immediately.

## User stories covered

6, 7
