# Issue 08 — rcm_core runtime-imports uit views + gate-test

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice90-consolidatie-edit-unificatie/PRD.md` (slice 90).

## What to build

**Tracer bullet** voor UI/kern-decoupling (werkstroom C): views importeren
`rcm_core.*` niet meer op runtime; de adapterlaag is de enige toegang tot de
kern (AGENTS.md MUST).

End-to-end:

- Alle runtime-imports van `rcm_core.*` in `rcm_desktop/views/` opgelost:
  typing-only achter `TYPE_CHECKING`, of verplaatst naar een adapter-functie
  (kleinste fix per geval).
- Betrokken views o.a. werkruimte-venster, entity-grid-panel,
  nb-effect-filter-combo, rcm-cost-parity-dialog, library-explorer,
  report-generation, input-entity-filter-proxy.
- Nieuwe gate-test scant `rcm_desktop/views/` op verboden runtime-kernimports
  (prior art: `test_slice62_panel_gate.py`).
- Geen wijziging aan user-visible gedrag.

## Acceptance criteria

- [ ] Geen runtime `from rcm_core` / `import rcm_core` in `rcm_desktop/views/`
      (typing-only imports toegestaan).
- [ ] Gate-test faalt bij nieuwe runtime-kernimports in views.
- [ ] Adapterlaag exposeert de benodigde types/data voor views zonder kern-leak.
- [ ] Bestaande view- en adapter-tests blijven groen.
- [ ] AGENTS.md UI/kern-decoupling MUST is hersteld.

## Blocked by

- Issue 07 (`.scratch/rcm-desktop-slice90-consolidatie-edit-unificatie/issues/07.md`)
  — werkruimte-venster (grootste leak-bron) eerst verkleind.

## User stories covered

15, 16, 18
