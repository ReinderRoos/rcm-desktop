# Issue 11 — FM-detail workspace binding extractie

**Type:** AFK  
**Triage:** `done`
**Outcome:** `fm_detail_workspace_binding.py`; venster delegeert FM render — `test_slice105_fm_detail_binding.py`

## Parent

PRD: `.scratch/rcm-desktop-slice105-werkruimte-ux-polish/PRD.md`

## Architectuur

Deepening candidate **1** (RenderPlan bindings): extract FM bind-logica uit
`ResultsWorkspaceWindow` naar `fm_detail_workspace_binding.py` — patroon
`simulation_workspace_binding.py` / `input_entity_grid_binding.py`.

## What to build

Nieuwe binding module die vanuit `_apply_render_plan` wordt aangeroepen:

- `apply_fm_detail_render(host, plan, snapshot)` — single-run tabel/diagram/inspector
- `apply_fm_compare_render(host, plan, snapshot)` — compare kolommen + diagram
- View-mode sync, inspector bind, chart `set_presentation` — allemaal in binding
- Venster behoudt: signal wiring, `_open_fm_editor`, session lifecycle

Doel: locality voor FM-UX; venster LOC-ratchet richting ≤2000 (slice 62 gate).

Start: `/tdd slice 105 issue 11`

## Acceptance criteria

- [ ] `fm_detail_workspace_binding.py` bestaat; venster delegeert FM render
- [ ] Geen `_apply_fm_*` / `_render_fm_*` methoden meer in venster (of dunne forwarders)
- [ ] `test_slice105_fm_detail_binding.py` groen
- [ ] `test_slice104_*`, `test_slice102_*` regressie groen

## Blocked by

Issue 10

## User stories covered

13
