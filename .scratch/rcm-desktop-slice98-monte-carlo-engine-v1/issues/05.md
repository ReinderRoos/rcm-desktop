**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
Implement `SimulationRunner` on shared `BackgroundRunner` (prior art: `RunRunner`). Emit progress during mocked slow engine; cooperative cancel sets job to `cancelled` and **discards partial results** (empty MC slot). Second `start()` while busy returns False. Extend `SimulationJob` / `build_simulation_presentation` for running/done/cancelled states with progress % and seed in `status_label`.

## Acceptance criteria
- [x] Progress updates emitted during long MC run (pytest-qt)
- [x] Cancel → job `cancelled`, no `result_ready` with done MC payload
- [x] After cancel, session MC slot empty (no partial bands)
- [x] Busy guard blocks overlapping jobs
- [x] `build_simulation_presentation` shows seed and percentage while running and after done
- [x] Cancel recovery: subsequent analytical run still works (session not corrupted)

## Outcome
`SimulationRunner` + `AppState.set_last_mc_run`; cancel discards via `run_monte_carlo` cancelled status. Tests: `tests/test_slice98_simulation_runner.py` (5 passed).

## Blocked by
Issue 04

## User stories covered
20, 21, 23, 24, 25, 44
