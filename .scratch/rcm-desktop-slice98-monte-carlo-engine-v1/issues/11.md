**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
Consolidate slice 98 tests in `tests/test_slice98_monte_carlo.py` (and extend slice 95 / aging / background_runner tests as needed). Document suggested CI subset in issue body or PRD cross-ref. Explicit tests: cancel leaves no MC result on session; Run-modus switch after cancel; mode-switch preserves dual namespaces.

## Acceptance criteria
- [x] `test_slice98_monte_carlo.py` covers adapter integration seams from PRD Testing Decisions
- [x] CI subset command documented (slice 98 + 95 + aging MC + background_runner)
- [x] Cancel → empty MC slot test present
- [x] MC cancel → switch analytical → last analytical still available
- [x] `CACHE_INPUTS_VERSION` bumped only if shared analytical input hashing changes (document decision in test or commit message)

## Outcome
CI subset documented in `test_slice98_monte_carlo.py` footer; cancel/namespace tests added. `CACHE_INPUTS_VERSION` unchanged (MC-only paths). Full slice-98 subset: 44 passed.

## Blocked by
Issues 08, 09, 10

## User stories covered
43, 44, 45
