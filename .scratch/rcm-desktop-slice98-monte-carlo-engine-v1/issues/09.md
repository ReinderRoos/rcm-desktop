**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
Replace `simulation_workspace_binding` stub with live binding to active `SimulationJob` from `SimulationRunner`: progress label, effective seed in statusstrip during and after run, cancel control visible while busy. Cancel invokes runner cancel and clears partial presentation. Idle MC modus shows “niet gestart” / last completed seed as appropriate.

## Acceptance criteria
- [x] During MC run, statusstrip shows progress percentage
- [x] Effective seed visible during and after completed run
- [x] Cancel button visible while busy; triggers cancel and updates status to cancelled
- [x] After cancel, FM-resultaten show empty/“geannuleerd” state (no bands)
- [x] Extends slice 95 status-label test to use live runner mock, not seed=42 stub

## Outcome
Live `simulation_workspace_binding` + cancel button; stub seed=42 removed. Tests: `tests/test_slice95_monte_carlo.py` updated (7 passed).

## Blocked by
Issues 05, 06

## User stories covered
20, 21, 22
