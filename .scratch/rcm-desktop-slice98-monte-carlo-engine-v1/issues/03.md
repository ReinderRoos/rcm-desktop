**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
For each simulation iteration, run PM/REV/CN task execution with the same semantics as the analytical engine; apply **task-group dedup** per iteration with ordering parity to `deduplicate_parallel_fm_pm_costs`. CM costs and downtime remain deterministic given drawn failure counts (explicitly no PR3 cost-parameter sampling). Synthetic two-FM / shared task-group fixture proves dedup is not double-counted per path.

## Acceptance criteria
- [x] PM/REV/CN affect each path’s cost and downtime totals
- [x] Two-FM shared task-group: MC PM cost per path ≤ undeduplicated upper bound; matches analytical PM total when failures are fixed
- [x] Task-group dedup ordering mirrors analytical post-pass
- [x] No sampling from `import_settings` error columns (PR3 guard test or comment in acceptance)
- [x] Full-project MC run completes on small fixture with all FM types represented

## Blocked by
Issue 02

## Outcome
Per-iteration `_simulate_iteration` with shared `counted_groups` across FM order. Test `test_shared_task_group_pm_not_double_counted_per_path` on slice-43 fixture.

## User stories covered
30, 31, 32
