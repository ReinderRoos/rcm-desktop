**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
In MC Run-modus, FM-resultaten table reads `FMMCResultRow` and shows **FM-onzekerheidsband** for kosten, downtime, and faalgebeurtenissen (P10/P50/P90 — implementer picks readable v1 layout aligned with slice 80 column patterns, e.g. P50 primary with P10–P90 tooltip or triplet sub-columns). **NMF** and **RF** columns unchanged. Per jaar / per LCC schaal-toggle applies to band columns where applicable. Clear visual distinction MC bands vs analytical point values. NB-effectfilter: in MC v1 bands reflect total FM downtime; document simplified filter semantics in UI copy or tooltip.

## Acceptance criteria
- [x] MC mode shows P10/P50/P90 for all three metrics per faalwijze row
- [x] Analytical mode still shows point estimates (unchanged)
- [x] NMF and RF columns stable across Run-modus switch
- [x] Schaal-toggle affects band presentation consistently with analytical FM table
- [x] MC vs analytical mode visually distinguishable (header, badge, or column label)
- [x] Minimal pytest-qt smoke: at least one band value visible with mocked `MCRunResult`

## Outcome
`FMMCResultsTableModel` + `build_fm_detail_view` MC branch; P50 display + P10–P90 tooltip. Tests: `tests/test_slice98_fm_mc_ui.py` (3 passed).

## Blocked by
Issue 06

## User stories covered
6, 8, 9, 10, 11, 12, 13, 14
