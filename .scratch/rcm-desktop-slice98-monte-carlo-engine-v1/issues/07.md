**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
Enable editing of `monte_carlo_n` (default 10.000) and optional `monte_carlo_seed` in modelinstellingen dialog (remove disabled state and “coming soon” tooltip). Wire through `model_settings_service` draft/commit; `monte_carlo_n >= 100` via existing `CFG_MC_N` validator; changes set `requires_rerun`. RCM-Cost import round-trip for `RcmNoSimulations` / `RcmRandomNoSeed` unchanged. Next MC run reads committed N/seed.

## Acceptance criteria
- [x] MC fields editable in dialog; values persist on OK
- [x] N < 100 blocked with existing validator message
- [x] Changing N or seed enables **Herbereken** / `requires_rerun`
- [x] Import fixture still populates `monte_carlo_n` / `monte_carlo_seed` correctly
- [x] Adapter unit tests for draft/commit and validation (no widget internals)

## Outcome
`model_settings_service` + dialog enabled MC fields. Tests: `tests/test_slice98_model_settings.py` (5 passed).

## Blocked by
Issue 04

## User stories covered
15, 16, 17, 18, 19
