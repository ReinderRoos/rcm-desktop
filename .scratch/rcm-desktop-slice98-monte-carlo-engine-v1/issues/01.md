**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
Introduce Qt-free `MetricBand` / `FMMCResult` and a `SimulationEngine` entry that simulates N paden for a **random** faalwijze: trek faalmomenten, bereken deterministische CM-kosten/downtime/faalgebeurtenissen per pad, aggregeer P10/P50/P90. Expose `progress_cb` and `cancel_check` hooks on the engine API (cooperative cancel between iterations). Core tests: fixed seed → stable percentiles; controlled setup (σ→0 or N=1) → P50 ≈ analytische `FMResult`.

## Acceptance criteria
- [x] `FMMCResult` holds P10/P50/P90 for kosten, downtime_hr, failures plus `n_completed` and effective `seed`
- [x] Seeded MC over small N is bit-stable in pytest
- [x] P50 aligns with analytical point metrics within tolerance for deterministic-limit fixture
- [x] Engine has no Qt imports and performs no disk I/O
- [x] `cancel_check` stops further iterations; adapter contract documented for “no aggregate on cancel” (tested in issue 05)

## Blocked by
None - can start immediately

## Outcome
`rcm_core/simulation_engine.py`: `MetricBand`, `FMMCResult`, `SimulationEngine.run` with random-FM paths, progress/cancel hooks. Tests: `tests/test_slice98_monte_carlo.py` (4 tests green).

## User stories covered
27, 35, 39, 41
