**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
Add thin `simulation_engine_service` over core `SimulationEngine`; wrap output in `MCRunResult`. Wire `SimulationResultStore` / session so MC results live in a **separate namespace** from analytical `RunResult` (analytical slot untouched). Implement `build_mc_fm_rows` producing `FMMCResultRow` with `MetricBand` fields. Seed policy: `monte_carlo_seed` empty → draw random 32-bit seed at job start; persist effective seed on job and aggregate.

## Acceptance criteria
- [x] `MCRunResult` attach/detach on session without overwriting `RunResult.fm_core_results`
- [x] `build_mc_fm_rows` row count = FM count; P10 ≤ P50 ≤ P90 for sane inputs
- [x] `SimulationResultStore.analytical_run_id` preserved when `mc_job_id` updates
- [x] Empty seed → nondeterministic across runs; fixed seed → reproducible `MCRunResult`
- [x] Core validation failures map to `UserFacingError`
- [x] Qt-free tests extend slice 95 MC patterns

## Blocked by
Issue 03

## Outcome
`rcm_desktop/adapter/simulation_engine_service.py` + `ProjectSession.mc_run` slot. Tests: `tests/test_slice98_simulation_adapter.py` (6 tests green).

## User stories covered
36, 38, 16, 22
