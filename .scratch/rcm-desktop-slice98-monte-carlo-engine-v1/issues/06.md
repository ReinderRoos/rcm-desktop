**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
Branch werkruimte **Start analyse** on `RunMode`: analytical → existing `RunRunner`/`run_service`; Monte Carlo → `SimulationRunner`. Remove slice-95 stub that fabricates jobs on combo change. Mode switch without re-run shows last results for the selected modus (analytical points vs MC bands). Filter/navigation changes during idle MC must **not** spawn a new `SimulationJob` (no derived-refresh coupling).

## Acceptance criteria
- [x] `RunMode.MONTE_CARLO` + Start analyse invokes MC runner, not analytical run
- [x] Switch MC → analytical without re-run shows last analytical `RunResult` if present
- [x] Switch analytical → MC without re-run shows last MC bands if present
- [x] Combo change alone does not create fake jobs or trigger runs
- [x] NB-effectfilter / view navigation during idle does not start MC job
- [x] Run-modus switch after MC cancel: analytical run still available and runnable

## Outcome
`simulation_run_binding.py` + `simulation_workspace_service.py`; stub removed. Tests: `tests/test_slice98_simulation_run_binding.py` (6 passed).

## Blocked by
Issue 05

## User stories covered
1, 2, 3, 5, 26, 37, 45
