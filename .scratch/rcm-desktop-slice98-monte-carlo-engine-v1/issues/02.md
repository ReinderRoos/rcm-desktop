**Type:** AFK  
**Triage:** done

## Parent
PRD: `.scratch/rcm-desktop-slice98-monte-carlo-engine-v1/PRD.md` (slice 98 Monte Carlo engine v1).

## What to build
Extend the path simulator to all `aging_distribution` variants (normal, truncated_normal_0, weibull_2p) using existing distribution SSOT. Include REV schedules and `repair_quality` rejuvenation per pad. Respect `aw_mc_lifecycle_horizon`, `modeljaar`, and PBS multiplicity/effective bouwjaar the same way the analytical run does. Extend `test_aging_monte_carlo` pattern: MC mean failures ≈ analytical `expected_failures` within established tolerance.

## Acceptance criteria
- [x] All three aging distributions participate in MC paths
- [x] REV + repair_quality fixture matches analytical bucket totals within ~5% tolerance
- [x] `aw_mc_lifecycle_horizon` affects lifecycle end consistently with analytical engine
- [x] Multiplicity from PBS hierarchy applied per path
- [x] Aging parity tests green alongside existing `test_aging_monte_carlo`

## Blocked by
Issue 01

## Outcome
Extended `SimulationEngine` with aging path simulator (normal/truncated/weibull, REV, Poisson renewal). Parity tests in `tests/test_slice98_monte_carlo.py`; existing `test_aging_monte_carlo.py` still green.

## User stories covered
28, 29, 33, 34, 40
