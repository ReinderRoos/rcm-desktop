# Issue 05 — Display-seam bijdragen-waarden

**Triage:** ready-for-agent  
**Type:** AFK  
**Prioriteit:** P2 (architectuur — blocker voor issue 03)

## Parent

PRD: `.scratch/rcm-desktop-slice72-top10-ux-verfijning/PRD.md` (slice 72, issue 05).

## What to build

Qt-vrije module `contribution_display_service` met gedeelde formattering voor bijdragen-waarden:

- `format_contribution_value(value, metric, presentation)` — waardekolom-semantiek (nu in `ContributionTableModel._format_value`).
- `format_contribution_bar_annotation(row, metric, presentation)` — compacte string voor staafdiagram (waarde, optioneel aandeel).

`ContributionTableModel` delegeert (zolang tabel bestaat) of wordt verwijderd in issue 02. Geen Qt-imports in de seam.

## Acceptance criteria

- [ ] Unit tests voor alle drie metrics × NB uren/% × kosten × faalmomenten.
- [ ] Eén implementatie; geen gedupliceerde format-logica in chart-widget.
- [ ] **Deletion test**: verwijderen van seam zou format-bugs in chart én tabel scheiden — seam verdient zijn plek.
- [ ] CONTEXT.md: korte vermelding display-seam onder Resultatenwerkruimte (optioneel in dit issue of 04).

## Blocked by

None — kan direct starten.
