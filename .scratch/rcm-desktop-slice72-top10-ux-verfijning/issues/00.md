# Issue 00 — NB-effectfilter dropdown opent bij klik

**Triage:** ready-for-agent  
**Type:** AFK  
**Prioriteit:** P0

## Parent

PRD: `.scratch/rcm-desktop-slice72-top10-ux-verfijning/PRD.md` (slice 72, issue 00).

## What to build

De multi-select NB-effectfilter in de Bijdragen-toolbar moet bij klik op het veld de dropdown openen en checkbare effectklassen tonen. Selectie wijzigt `EffectNbFilterSet` in workspace state en triggert herberekening van Top 10 (bestaande slice 71-keten).

Technische richting: read-only line edit op editable combo + `showPopup()` bij muisklik; optioneel `FocusPolicy` zodat het veld zich als knop gedraagt.

## Acceptance criteria

- [ ] Klik op het filterveld opent de popup (pytest-qt of widget-test).
- [ ] Items zijn checkbaar; label toont "Alle NB-effecten" of "N NB-effect(en)".
- [ ] `filter_changed` emit bij toggle; workspace state synchroniseert.
- [ ] Bestaande slice 71-postfix tests blijven groen.

## Blocked by

Slice 71 afgerond.
