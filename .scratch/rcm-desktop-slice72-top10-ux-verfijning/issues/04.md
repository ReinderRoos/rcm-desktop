# Issue 04 — NB-effectfilter end-to-end op Top 10 bijdragen

**Triage:** ready-for-agent  
**Type:** AFK  
**Prioriteit:** P1

## Parent

PRD: `.scratch/rcm-desktop-slice72-top10-ux-verfijning/PRD.md` (slice 72, issue 04).

## What to build

Verifieer en harden dat `EffectNbFilterSet` de Top 10-bijdragen end-to-end beïnvloedt: ranking, `value` per rij, en chart-labels (via issue 03). Voeg integratietests toe op `build_contribution_rows` + cache-invalidatie wanneer filter actief is.

Dekt faalwijze-bron (standaard na issue 01) en PBS-scope bij boomselectie. Geen nieuwe businesslogica tenzij test een gap blootlegt.

## Acceptance criteria

- [ ] Filter op één NB-effectklasse: som `value` ≈ gefilterde NB voor die klasse (golden fixture).
- [ ] Ranking wijzigt meetbaar t.o.v. "alle effecten".
- [ ] Cache miss bij actief filter (bestaande test blijft groen).
- [ ] Chart-labels reflecteren gefilterde waarden (niet ongefilterde totalen).

## Blocked by

Issue 00 (dropdown bruikbaar), issue 03 (labels tonen juiste eenheid).
