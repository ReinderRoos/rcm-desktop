# Issue 01 — Taakgroep-dropdown + normalize_optional_fk

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice49-fm-editor-model-waarschuwingen/PRD.md`

## What to build

Voeg `normalize_optional_fk` toe zodat `"None"`, `"null"` en lege strings naar `null` gaan. Vervang vrije tekst in de PM-tabelkolom `task_group_id` door een dropdown met "(geen)" en bestaande taakgroepen. Normaliseer bij inlezen uit de tabel en in de editing FK-validatie.

## Acceptance criteria

- [ ] `normalize_optional_fk("None")` is `None`; geldige id blijft behouden
- [ ] PM-tabel toont "(geen)" i.p.v. `None` voor lege taakgroep
- [ ] Commit met lege taakgroep faalt niet op FK `"None"`
- [ ] Unit-tests op normalisatie en commit-pad

## Blocked by

None - can start immediately
