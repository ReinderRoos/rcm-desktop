# Issue 02 — Meekoppelkansen read-only in LCC what-if

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice39-meekoppelkansen-planning-2a/PRD.md` (slice 39).

## What to build

End-to-end **leespad**: analist ziet meekoppelkansen in de werkruimte zonder apply-acties.

**Adapter (Qt-vrij, test-first):**

- Nieuwe **meekoppelkansen discovery service**: input `RCMProject`, `window_years=2`; output immutable rijen (`element_naam`, `pm_a`, `pm_b`, `pbs_a`, `pbs_b`, `jaar_a`, `jaar_b`, `jaar_delta`, `reden`).
- Regels (RCM1-parity): alleen REV; `interval_jaar > 0`; FM/PBS/`element_naam` aanwezig; due-jaar = `int(round(interval_jaar))`; paar alleen bij zelfde `element_naam` en `jaar_delta ≤ window_years`; geen dubbele paren (canonieke pm_id-volgorde).
- Geen import `meekoppelkansen_v1.py`; geen pandas.

**Werkruimte-UI:**

- Meekoppelkansen-paneel in LCC-modus, **alleen zichtbaar als What-if planning actief**.
- Buiten what-if: paneel verborgen of disabled met hint (“Schakel what-if in…”).
- Tabel: element, PM A/B, jaar A/B, delta, reden; empty-state bij geen suggesties.
- Geen Preview/Toepassen-knoppen in dit issue (komen in issue 03).
- Geen wijzigingen ValidateWindow.

**Tests:** unit tests op discovery-regels (REV-only, element-match, venster, interval, paar-uniekheid) op synthetic/minimal fixture.

## Acceptance criteria

- [x] Discovery service levert correcte paren op testproject (REV + zelfde element + delta ≤ N).
- [x] Non-REV, verschillende elementen, delta > N, interval ≤ 0 → geen paar (unit tests groen).
- [x] Werkruimte toont suggestietabel iff LCC + what-if actief.
- [x] What-if uit → geen apply-pad; hint of verborgen paneel.
- [x] Empty-state wanneer geen suggesties.
- [x] Project wisselen → geen stale rijen uit vorig project.
- [x] Views importeren geen `rcm_core` (typing-only).

## Blocked by

- Issue 01 (ADR-0005 + CONTEXT scrub-list).
