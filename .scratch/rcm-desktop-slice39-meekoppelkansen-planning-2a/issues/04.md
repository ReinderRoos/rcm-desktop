# Issue 04 — Venster N, anker-toggle, pytest-qt smoke + handoff

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice39-meekoppelkansen-planning-2a/PRD.md` (slice 39).

## What to build

Afronding slice 39: configureerbare discovery, flexibele preview, integratietests en gate-documentatie.

**UI:**

- Spinbox **tijdsvenster N** (range bijv. 1–5, default 2); wijziging → suggestietabel refresh.
- In preview: **anker-toggle** vroegste/laatste due-jaar vóór apply (issue 03 default blijft `"earlier"`).
- Na apply: suggestielijst refresh (vervulde/gewijzigde kansen reflecteren overlay).

**Tests:**

- pytest-qt smoke: what-if aan → suggestietabel zichtbaar → preview → apply → overlay/LCC-indicator update.
- What-if uit → apply-knop disabled of afwezig.
- Unit test: N=1 vs N=3 wijzigt aantal paren op fixture.

**Handoff:**

- `KANBAN_HANDOFF.md` in slice-map: wat geleverd, Haarlem handchecklist (what-if, suggesties, preview, apply, reset, geen regressie LCC/shift).
- Gate 2a→2b: checklist groen vóór slice 40 start.

## Acceptance criteria

- [x] Venster N wijzigt discovery-resultaat (getest).
- [x] Anker-toggle in preview wijzigt shift-richting vóór apply (getest via adapter of UI).
- [x] pytest-qt smoke groen op geschikte fixture (of gemockte adapter waar nodig).
- [x] Apply na N-wijziging/refresh: lijst consistent met overlay-state.
- [x] `KANBAN_HANDOFF.md` met Haarlem-checklist aanwezig.
- [x] Bestaande slice 36–38 LCC/what-if tests blijven groen.

## Blocked by

- Issue 03 (preview + apply overlay-shift).
