# Issue 05 — Kruis-entiteit-regels als waarschuwing (ADR-0015)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice88-invoerbevindingen-grid/PRD.md` (slice 88).
ADR: `docs/adr/ADR-0015-ernst-dualiteit-invoerbevindingen.md`.

## What to build

Kruis-entiteit-regels uit `validate_project` zijn **waarschuwingen** in het
grid maar blijven **harde fouten** bij run/Validate (ADR-0015).

End-to-end:

- `input_grid_findings` markeert kruis-entiteit-codes met `severity=warning`
  in `edit_errors`.
- Typisch voorbeeld: `FM_NMF_REQUIRES_TEST` op faalwijzen (`is_evident` of
  sleutelkolom-fallback).
- `materialize_for_run` blokkeert niet op deze grid-waarschuwingen; run-pad
  gebruikt nog steeds `validate_project` hard.

TDD: dualiteit-test (grid geel, validate hard), dan integratie met NMF-fixture.

## Acceptance criteria

- [ ] NMF-faalwijze zonder IN/TST-taak: gele markering in grid, geen
      materialize-blokkade.
- [ ] Zelfde project: Validate-knop / `validate_project` meldt nog steeds
      `FM_NMF_REQUIRES_TEST` als fout.
- [ ] ADR-0015-classificatie is expliciet in `input_grid_findings` (geen
      impliciete heuristiek).

## Blocked by

- `.scratch/rcm-desktop-slice88-invoerbevindingen-grid/issues/03.md`
