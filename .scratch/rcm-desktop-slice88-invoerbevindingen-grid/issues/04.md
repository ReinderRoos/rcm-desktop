# Issue 04 — Enkel-rij projectvalidatieregels als fout

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice88-invoerbevindingen-grid/PRD.md` (slice 88).

## What to build

Enkel-rij-regels uit `validate_project` verschijnen als rode invoerbevindingen
in het grid (zelfde ernst als bij run/Validate).

End-to-end:

- `input_grid_findings` classificeert enkel-rij-regelcodes en mapt ze naar
  entiteit + kolom (bv. `FM_RANDOM_SIGMA_NONZERO` → `sigma_jaar`).
- Injectie bij buffer-brede hervalidatie met `severity=error`.
- Run-/Validate-semantiek van `validate_project` blijft ongewijzigd.

TDD: classificatie-metadata + mapping-test, dan grid-integratie met bekende
regel `FM_RANDOM_SIGMA_NONZERO`.

## Acceptance criteria

- [ ] Faalwijze met `failure_type=random` en `sigma_jaar > 0` toont rode
      markering op `sigma_jaar` na hervalidatie.
- [ ] Negatieve `cost_cm_eur` toont rode markering op die kolom.
- [ ] `validate_project` gedrag bij run is ongewijzigd (regressietest).
- [ ] Alleen regels voor entiteiten met een Input-view landen in het grid.

## Blocked by

- `.scratch/rcm-desktop-slice88-invoerbevindingen-grid/issues/01.md`
