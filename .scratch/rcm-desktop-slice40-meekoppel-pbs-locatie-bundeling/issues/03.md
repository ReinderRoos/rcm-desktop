# Issue 03 — Compacte werkruimte-tabel + copy

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice40-meekoppel-pbs-locatie-bundeling/PRD.md` (slice 40).

## What to build

**Werkruimte (LCC + what-if only):**

- Sync paneel op `discover_meekoppel_locations`.
- Tabelkolommen: **boompad, PBS-id, # REV, due-bereik, span** — geen lange REV-lijst in tabel.
- Preview-dialog: default **laatste** anker aangevinkt; body toont alle PM-verschuivingen.
- Apply zonder preview: gebruikt default **laatste** (`_meekoppel_last_anchor` init `"later"`).
- Help-tekst: PBS-locatie, due = levensduur-jaar, bundelen per rij.

**Messages:** NL-koppen en preview-strings bijwerken.

Geen ValidateWindow.

## Acceptance criteria

- [ ] Tabel toont boompad + PBS-id per locatiegroep.
- [ ] Preview toont volledige PM-lijst; tabel blijft compact.
- [ ] Dialog + code default anker = laatste.
- [ ] What-if uit → hint/disabled (slice 39-gedrag intact).
- [ ] Views geen `rcm_core` (typing-only).

## Blocked by

- Issue 02 (preview/apply API).
