# Issue 01 — ADR-amendement + PBS-pad + locatie-discovery

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice40-meekoppel-pbs-locatie-bundeling/PRD.md` (slice 40).

## What to build

**Documentatie:**

- Amend **`docs/adr/ADR-0005-meekoppelen-onderhoud.md`**: discovery op **`pbs_id`** (niet `element_naam`); default anker **laatste**; RCM1 `element_naam`-collisie als historische noot; slice 40 = Planning-2b-locatiebundeling.

**Adapter (Qt-vrij, test-first):**

- **PBS-pad label service:** `path_label(project, pbs_id)` → string via `parent_pbs_id`-keten; segment = `bouwdeel_naam` of fallback `pbs_id`; separator consistent (bijv. ` › `).
- **Discovery refactor:** `MeekoppelLocationGroup` met `pbs_id`, `path_label`, `tasks`, min/max/span due; `discover_meekoppel_locations(project, window_years=2)`.
- Regels: REV-only; `interval_jaar > 0`; FM/PBS aanwezig; groep iff ≥ 2 REV opzelfde `pbs_id` en span ≤ N.
- **Verwijder paar-only legacy** in dit issue: `MeekoppelSuggestion`, `discover_meekoppelkansen` (UI gebruikt die niet meer sinds slice 39 element-groepen).
- **Behoud tijdelijk** `MeekoppelElementGroup` / `discover_meekoppel_element_groups` + bestaande element-apply tot issue 02 de locatie-apply levert (geen half-broken UI tussen slices).

Geen werkruimte-UI in dit issue.

## Acceptance criteria

- [ ] ADR-0005 gewijzigd en consistent met PRD.
- [ ] Pad-service unit tests: keten root→blad, lege bouwdeel fallback.
- [ ] Discovery tests: twee `pbs_id` metzelfde `element_naam` → twee groepen.
- [ ] Discovery tests: span > N → geen groep; ≥ 2 REV span ≤ N → één groep.
- [ ] Geen Qt-imports in nieuwe/gewijzigde adapter-modules.

## Blocked by

None.
