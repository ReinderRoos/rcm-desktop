# Issue 02 — Preview/apply locatiegroep + legacy weg

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice40-meekoppel-pbs-locatie-bundeling/PRD.md` (slice 40).

## What to build

**Adapter:**

- `preview_meekoppel_location` / `apply_meekoppel_location` op `MeekoppelLocationGroup`.
- Default anchor **`later`**; preview retourneert alle `MeekoppelShiftMove` voor PM’s die verschuiven.
- Apply: all-or-nothing validatie SVO/WET; volledige `anchor_years`-update in één stap.
- **Verwijder element-groep legacy** (eindstaat één datamodel): `MeekoppelElementGroup`, `discover_meekoppel_element_groups`, `preview_meekoppel_group`, `apply_meekoppel_group`, en eventuele resterende paar-functies (`preview_meekoppel_suggestion`, `apply_meekoppel_suggestion`).

**Tests:** preview default later; earlier toggle; multi-PM apply; overlay `change_count`; SVO-blokkade.

Geen UI in dit issue.

## Acceptance criteria

- [ ] Apply verschuift alle niet-anker REV’s in groep naar target due-jaar.
- [ ] Default anchor later in preview/apply (geen `"earlier"` default).
- [ ] Bestaande overlay-offsets meegenomen in effectief due-jaar.
- [ ] Geen publieke paar-API meer in adapter.
- [ ] Unit tests groen.

## Blocked by

- Issue 01 (locatie-discovery + types).
