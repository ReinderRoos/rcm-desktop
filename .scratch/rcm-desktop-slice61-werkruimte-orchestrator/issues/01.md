## Parent

PARENT_ISSUE_URL

## What to build

Eerste tracer voor slice 61: nieuwe **`ResultsWorkspaceOrchestrator`** (skeleton) met **`plan_ui_sync(prev, current) -> WorkspaceUiSyncPlan`**.

Geneste frozen dataclasses: `BijdragenToolbarPlan`, `LccToolbarVisibilityPlan`, `FmToolbarPlan`, `CompareChromePlan`, `CollapsePanelsPlan`.

Logica uit workspace state-changed (toolbar-zichtbaarheid, combo-indices, compare-toggle sync, panel-collapse) verhuist naar de adapter. De view krijgt **`_apply_ui_sync(plan)`**; `_ensure_whatif_for_meekoppel()` blijft view-side.

**Niet in deze slice:** meekoppel tabeldata, LCC overlay-chrome (`_sync_lcc_chrome`), render/routing.

## Acceptance criteria

- [ ] `plan_ui_sync` unit-testbaar zonder PySide6 (moduswissel, scope-change → `refresh_kpi`, LCC toolbar visibility)
- [ ] View: workspace state-changed delegeert ui-deel aan orchestrator
- [ ] Geen UX-regressie: bestaande resultatenwerkruimte window tests groen
- [ ] `ResultsWorkspaceController` ongewijzigd; slice 53 issue 06 orchestrator tests groen

## Blocked by

None - can start immediately.
