## Parent

PARENT_ISSUE_URL

## What to build

Tweede tracer: **`plan_render(current, ctx) -> RenderPlan`** roept bestaande modus-builders aan. Tagged union met bestaande view-DTOs (`FMDetailView`, `BijdragenView`, `LCCView`, compare panels). Bij `split_depth == all_splits`: `ctx.render_index.on_workspace_state_reset()` vóór builders.

**`WorkspaceRenderContext`**: session, compare_slots, project_total_presentation, render_index, prev_lcc_snapshot.

**`plan_workspace_tick`**: compose ui_sync + render + afgeleide split_depth via bestaande render-scope logica.

View: **`_apply_render_plan(plan)`** vervangt routing in detail-rerender. Compare slot complete en presentation rebuild roepen alleen `plan_render` aan (`on_slot_updated` blijft in compare handler). FM selectie-restore blijft view na bind. `_sync_lcc_chrome` blijft bij LCC bind.

## Acceptance criteria

- [ ] Orchestrator unit tests: empty, fm, bijdragen, lcc, compare_placeholder, scope-change → cache reset
- [ ] View render-pad gebruikt orchestrator; detail-rerender doet geen modus-routing meer
- [ ] ADR-0007: compare placeholders gedrag gelijk
- [ ] Slice 37 en workspace run tests groen

## Blocked by

CHILD_01_ISSUE_URL
