# Issue 01 — Herberekenen met passief-overlay in de motor

**Triage:** done  
**Superseded (gedeelte):** slice 30 issue 01 — na herberekenen blijven `disabled_pm_ids` behouden (vervangt post-run leegmaken uit dit issue).

**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice29-planning-overlay-motor/PRD.md` (slice 29).

## What to build

Wanneer **What-if planning** actief is en de gebruiker **Herbereken analyse** start, draait de motor op een **tijdelijke project-clone** zonder PM-taken in `disabled_pm_ids` (`pm_tasks` en `pm_effect_links` gefilterd). Opgeslagen JSON en edit-buffer blijven ongewijzigd. Bij what-if **uit** herberekent de run op het normale project (geen overlay).

Na een **geslaagde** run met overlay: **`disabled_pm_ids` leegmaken** (passief zit in `last_run`); **`anchor_years` behouden** (verschuivingen blijven presentatie-only). Bij run-fout blijft de overlay intact.

LCC-presentatie na zo’n run toont **één consistente curve** op `last_run` — geen misleidende baseline-vs-overlay-PM waarbij CM al uit de passief-run komt en baseline-LTAP nog volledige REV uit JSON toont.

```python
# Post-run overlay (PRD-contract)
def after_successful_overlay_run(overlay: PlanningOverlayState) -> PlanningOverlayState:
    return PlanningOverlayState(
        active=overlay.active,
        anchor_years=overlay.anchor_years,
        disabled_pm_ids=frozenset(),
    )
```

Verhoog `CACHE_INPUTS_VERSION` alleen als de run-invoer zich materieel wijzigt t.o.v. bestaand gedrag (clone-pad).

## Acceptance criteria

- [x] Met what-if aan en minstens één passieve taak gebruikt herberekenen de materialized clone; JSON ongewijzigd.
- [x] Met what-if uit negeert herberekenen `disabled_pm_ids` (baseline-project).
- [x] Na succes: `anchor_years` ongewijzigd; bij fout: overlay ongewijzigd. (`disabled_pm_ids` leegmaken → superseded door slice 30 #01: behouden.)
- [x] Fixture `one_fm_planning.rcm.json`: alle REV passief + herbereken → verwacht hoger correctief / lager PM in `last_run` (adapter-test).
- [x] LCC na run toont geen dubbele passief-laag (geen baseline/overlay-PM-vergelijking voor reeds gedraaid passief).
- [x] `tests/test_desktop_planning_overlay_state.py` en nieuwe materializer/run-tests groen.

## Blocked by

None — can start immediately (vereist slice 28 overlay + LCC-planning geleverd).
