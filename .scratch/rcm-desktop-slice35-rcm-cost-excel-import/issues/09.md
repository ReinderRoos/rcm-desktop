# Issue 09 — AW Enabled → planning-overlay seed

**Triage:** done  
**Type:** AFK  
**Commit:** `a939676` (2026-05-21)

## Parent

PRD: `.scratch/rcm-desktop-slice35-rcm-cost-excel-import/PRD.md` (slice 35 follow-up).

## What was built

1. **Import:** `isograph_import_service` verzamelt `aw_disabled_pm_ids` uit `RcmScheduledTasks` waar `Enabled=False` (zelfde `pm_id`-conventie als `PMTask`: `{Cause}|{TaskId}|{SubIndex}`).
2. **Persist:** lijst in `import_settings` op `.rcm.json` (via `normalize_import_settings`).
3. **Werkruimte:** `PlanningOverlayState.from_import_settings()`; seed na `reset_for_new_project()` in `ResultsWorkspaceWindow._seed_planning_overlay_from_import` — overlay **actief** wanneer de set niet leeg is, zodat **Start analyse** via `run_service` materialiseert zonder handmatige what-if-toggle.

## Acceptance criteria

- [x] CM-fixture: `aw_disabled_pm_ids` niet leeg; alle ids ∈ `pm_tasks` (235 taken op CM-fixture).
- [x] Synthetische sheet-test: alleen `Enabled=False` taak in disabled-lijst.
- [x] `test_import_overlay_seed.py` + `test_run_service_materializes_overlay_disabled_pm`.
- [x] pytest-subset groen (39 tests incl. overlay).

## Tests

```powershell
python -m pytest tests/test_isograph_import_service.py tests/test_import_overlay_seed.py `
  tests/test_desktop_run_service.py::test_run_service_materializes_overlay_disabled_pm -q
```
