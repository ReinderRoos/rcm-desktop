# Issue 01 — Import-capability matrix (AW → RCM2)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice35-rcm-cost-excel-import/PRD.md` (slice 35).

## What to build

Schrijf het **stap-3 deliverable**: import-capability matrix als markdown in deze slice-map (bijv. `IMPORT_MATRIX.md`).

End-to-end (documentatie):

- Alle 25 tabbladen van `RCMCostdata export_*.xlsx` met kolom **must v1 / should / won't / candidate feature**.
- Per **must**-kolom: AW-kolom → RCM2-veld → eenheid (uren/jaren/EUR) → opmerking.
- Vastleggen grill-me besluiten: geen gevolgkosten; RF → `fractie`; pruned PBS; uren÷8760; `import_settings` voor MC/PEnable; geen Labor/Spares.
- Sectie **open** expliciet verwijzen naar issue 02 (PM-spike).

**Niet in deze issue:** Python-code, mapper, UI.

## Acceptance criteria

- [x] Matrix-bestand bestaat en dekt alle tabbladen.
- [x] Must v1-lijst komt overeen met PRD-tabel.
- [x] Eenheden-regel (AW uren) en `LifeTime`/`FmMttf`-conversie zijn gedocumenteerd.
- [x] `cost_gevolg` en catalog-tabbladen staan op won't v1.
- [x] Layout-contract in `rcm_core/isograph_export_contract.py` + pytest (TDD fase 0).

## Blocked by

None — can start immediately.
