# Issue 07 — Navigatieboom PBS → functie → faalwijze

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice35-rcm-cost-excel-import/PRD.md` (slice 35).

## What to build

Vervang/uitbreid de PBS-sidebar zodat de **structuurboom** doorgaat tot **faalwijze-niveau** met Isograph-ID’s als labels.

End-to-end:

- `rcm_navigation_tree_builder` (Qt-vrij): uit `RCMProject` knopen PBS → functie → faalwijze.
- Qt-model voor sidebar in resultatenwerkruimte (vóór run: geen aggregaten op FM; `—` of lege totalen op PBS indien nodig).
- Selectie FM-knoop: filter FM-tabel / scope consistent met bestaande PBS-subtree-gedrag.
- Werkt met geïmporteerd project (Isograph-ID’s zichtbaar).

**Niet in deze issue:** Excel openen (issue 08).

## Acceptance criteria

- [x] Unit-tests op boomvorm en sortering.
- [x] pytest-qt: geladen import-project → boom toont FM onder functie onder PBS.
- [x] Bestaande run-gekoppelde PBS-aggregaten blijven werken wanneer run aanwezig.

## Blocked by

- Issue 05 (project met functies/faalwijzen; test via fixture of mock builder).
