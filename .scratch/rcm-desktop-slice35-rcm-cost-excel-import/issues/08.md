# Issue 08 — Desktop: Open RCM-Cost export + opslaan

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice35-rcm-cost-excel-import/PRD.md` (slice 35).

## What to build

Verticale afronding: analist kan een **RCM-Cost Excel-export** openen en als **`.rcm.json`** verder werken in RCM2.

End-to-end:

- Menu/actie “Open RCM-Cost export…” (ValidateWindow en/of main — volg bestaande open-project-patronen).
- Flow: bestand → issue 06 wizard → `validate_project` → opslaan atomisch (`save_service`) → project in app state → resultatenwerkruimte met issue 07-boom.
- `CONTEXT.md`: korte sectie **RCM-Cost import** (drie regels: bootstrap, geen gevolgkosten, PM-spike).
- Foutafhandeling: onleesbaar bestand, ontbrekende must-sheet, validator errors met NL-melding.

**Niet in deze issue:** export terug naar Excel, Monte Carlo UI.

## Acceptance criteria

- [x] pytest-qt smoke: mock xlsx → opgeslagen pad → project geladen (`tests/test_desktop_open_rcm_cost_export.py`).
- [x] Handmatig: `RCMCostdata export_Gaarkeuken` (gevuld) → import, analyse, Top10/FM/Tijdsplot OK. Leeg fixture: alleen structuur (geen volledige validatie).
- [x] Bestaande validate/run-flow ongewijzigd bruikbaar na import.
- [x] ADR + matrix + spike-gate gedocumenteerd in PRD afgevinkt vóór merge.

## Blocked by

- Issue 06 (wizard).
- Issue 07 (navigatieboom).
