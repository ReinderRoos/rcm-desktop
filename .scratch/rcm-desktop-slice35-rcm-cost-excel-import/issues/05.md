# Issue 05 — isograph_import_service (mapper, Qt-vrij)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice35-rcm-cost-excel-import/PRD.md` (slice 35).

## What to build

Deep module `isograph_import_service` + `isograph_excel_reader` in adapterlaag.

## Acceptance criteria

- [x] Tests groen zonder QApplication (`tests/test_isograph_import_service.py`).
- [x] Cause uit fixture → `Faalwijze` met `mttf_jaar` in jaren.
- [x] `import_settings` bevat MC-raw uit `Project` (subset).
- [x] IA-conflicten per `pbs_id` gerapporteerd.
- [x] PM-regels via `isograph_pm_import_rules.py`.

## Blocked by

- Issue 01, 02, 04.
