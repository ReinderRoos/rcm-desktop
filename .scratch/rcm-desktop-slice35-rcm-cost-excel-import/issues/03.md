# Issue 03 — ADR Excel-IO + import_settings-contract

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice35-rcm-cost-excel-import/PRD.md` (slice 35).

## What to build

Publiceer een ADR die Excel-import van de scrub-list haalt en het JSON-contract voor **`import_settings`** vastlegt.

End-to-end:

- ADR onder `docs/adr/`: motivatie bootstrap-import; geen Avsim-pariteit; asymmetrische export later.
- Contract: top-level `import_settings` op `.rcm.json`; `import_settings_schema_version`; pass-through onbekende keys; MC-kolommen uit `Project` en cause-onzekerheid.
- Verwijs naar `CONTEXT.md`-update (kan in issue 08 of hier): korte vermelding RCM-Cost import-pad.
- Update scrub-list referentie in README/CONTEXT indien project dat zo doet bij ADR’s.

**Niet in deze issue:** mapper-implementatie.

## Acceptance criteria

- [x] ADR bestaat en is reviewbaar (`docs/adr/ADR-0004-…`).
- [x] `import_settings`-vorm en versieveld zijn beschreven (`import_settings_contract.py` + tests).
- [x] Expliciet: gevolgkosten per effect blijven buiten RCM2.
- [x] Export subset naar AW = out of scope (latere slice).
- [x] `CONTEXT.md` bijgewerkt (Excel bootstrap vs scrub-list).

## Blocked by

None — can start immediately.
