# Issue 03 — `main.py` `aboutToQuit`-teardown (runner-vangnet)

**Triage:** ready-for-agent
**Type:** AFK
**Prioriteit:** P2

## Parent

PRD: `.scratch/rcm-desktop-slice78-nette-afsluitprocedure/PRD.md` (slice 78, issue 03).

## What to build

Een vangnet zodat lopende runners ook bij een afsluitpad buiten `closeEvent` netjes
stoppen.

- Verbind in `rcm_desktop/main.py` een `app.aboutToQuit`-handler die de actieve
  achtergrondrunners van het venster `cancel()`t (idempotent t.o.v. issue 02).
- Geen gedragsverandering voor de normale `closeEvent`-route; dit is puur
  defensief voor app-quit langs een ander pad.

## Acceptance criteria

- [ ] `aboutToQuit` cancelt actieve runners (geen achtergebleven QThreads).
- [ ] Dubbele cancel (closeEvent + aboutToQuit) is veilig (idempotent).
- [ ] Normale afsluiting verandert niet van gedrag.
- [ ] Getest met een fake/stub-runner of `tests/test_background_runner.py`-stijl.

## Blocked by

- Issue 02 (closeEvent-bedrading + runner-cancel).
