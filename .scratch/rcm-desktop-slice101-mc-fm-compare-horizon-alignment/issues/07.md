# Issue 07 — LCC MC compare without prior analytical run (HILT101 blocking)

**Triage:** `done`  
**Type:** AFK  
**Outcome:** `resolve_compare_slot_run` + `run_mode` on `build_lcc_compare_panels`; MC LCC rebuilds from `mc_run.fm_results`. Tests: `test_slice101_mc_lcc_compare_without_analytical.py`.
