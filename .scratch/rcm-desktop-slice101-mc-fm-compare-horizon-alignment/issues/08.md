# Issue 08 — MC RF column alignment (HILT101 non-blocking)

**Triage:** `done`  
**Type:** AFK  
**Outcome:** `build_mc_fm_rows` uses `_resolve_rf_for_fm` (same as analytical). Tests: `test_slice101_mc_rf_column.py`.
