# Issue 01 — Adapter: compact FM-tabel + metric-sync

**Type:** AFK  
**Triage:** `done`  
**Outcome:** Compact single-run FM via `FMSingleRunTableModel` + `build_faalwijze_single_run_presentation`; NMF/RF-toggle op single-run toolbar — `test_slice104_fm_single_run_compact_table.py`

## Parent

PRD: `.scratch/rcm-desktop-slice104-fm-single-run-presentatie/PRD.md`

## What to build

Generaliseer compare FM-presentatie voor **single-run**:

- Hergebruik of split `FMCompareTableModel` / `build_fm_compare_panels`-logica
  naar een single-run variant (`FmSingleRunTableModel` of gedeelde basis).
- **Eén metric-kolom** (Faalmomenten / Niet-beschikbaarheid / Kosten) volgt
  `contribution_metric` / toolbar-filter — zelfde semantiek als compare.
- **NMF / RF** kolommen default **uit**; toggle-state via toolbar-plan
  (`fm_nmf_rf_toggle_visible` analoog aan compare).
- Adapter-only presentatie-DTO; geen wijziging `rcm_core`.

Start: `/tdd slice 104 issue 01`

## Acceptance criteria

- [ ] Single-run FM-tabel toont standaard: FM-id, Bouwdeel, Omschrijving, **één metric**
- [ ] Metric wisselt bij filter-combobox (Faalmomenten/NB/Kosten)
- [ ] NMF/RF niet zichtbaar tot toggle aan
- [ ] Regressietest `test_slice104_fm_single_run_compact_table.py` groen
- [ ] Bestaande compare-tests (`test_slice102_*`, `test_slice100_compare_*`) groen

## Blocked by

None

## User stories covered

2, 3, 6
