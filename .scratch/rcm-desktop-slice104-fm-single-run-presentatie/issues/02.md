# Issue 02 — UI: tabel/diagram-toggle single-run FM

**Type:** AFK  
**Triage:** `done`  
**Outcome:** Single-run tabel/diagram-toggle + `FaalwijzeCompareBarChartWidget` single-series — `test_slice104_fm_single_run_diagram.py`

## Parent

PRD: `.scratch/rcm-desktop-slice104-fm-single-run-presentatie/PRD.md`

## What to build

Voeg **tabel / diagram** toggle toe aan single-run FM-toolbar (zoals compare):

- Toggle-knoppen in `FmToolbarPlan` + `workspace_toolbar_sync` voor
  `MODE_FM_DETAIL` (niet alleen compare-modus).
- Diagram: hergebruik `FaalwijzeCompareBarChartWidget` in single-series modus
  (één scenario, actieve metric, horizon/jaar-sync).
- Default view = **tabel** (consistent met compare).
- Verberg NMF/RF-toggle in diagram-modus (zoals compare).

## Acceptance criteria

- [ ] Toggle zichtbaar in single-run FM; **niet** in LCC-only context
- [ ] Diagram toont FM-rijen gesorteerd op actieve metric
- [ ] Horizon + Ø per jaar / jaarkiezer werken in diagram
- [ ] Regressietest `test_slice104_fm_single_run_diagram.py` groen
- [ ] `test_slice103_fm_detail_ui` — update: toggle **wel** zichtbaar in FM-detail

## Blocked by

Issue 01

## User stories covered

1, 6
