# Issue 05 — LCC achtergrond-warmup na run

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice38-lcc-snel-zichtbaar/PRD.md` (slice 38).

## What to build

Optionele achtergrond-warmup analoog aan `PresentationRebuildRunner`:

- Na geslaagde analyse-run: worker-thread bouwt default LCC-curve `(scope=None/project-total, filters=all, overlay=inactive)` in session-scoped `WorkspaceRenderIndex`.
- Gebruikt light path uit issue 03 waar beschikbaar.
- Cancel/ignore bij project-wissel of nieuwe run start.
- Geen UI-bindings in worker; alleen adapter + render index injectie.

Doel: eerste bezoek aan LCC-modus na run is vaak cache-hit.

## Acceptance criteria

- [ ] Na run success start warmup op achtergrond (niet op UI-thread).
- [ ] Moduswissel naar LCC binnen enkele seconden na run → curve cache-hit (spy: geen sync curve-build op main thread).
- [ ] Nieuwe run of project-wissel annuleert/ignoreert stale warmup.
- [ ] Geen regressie: contribution-only post-run (slice 37) blijft; warmup blokkeert run-completion niet.
- [ ] Adapter-test met mock worker of directe warmup-aanroep verifieert render-index populatie.

## Blocked by

- Issue 03
