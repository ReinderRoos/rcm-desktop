# Issue 02 — Curve-builder dedup (reconcile + inactive overlay)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice38-lcc-snel-zichtbaar/PRD.md` (slice 38).

## What to build

Optimaliseer `build_lcc_planning_curve_reconciled` zodat redundante werk verdwijnt:

1. **Reconciliatie:** maximaal **één** aanroep van `reconcile_planning_curve_pm_total` per curve-build (niet dubbel via interne `build_lcc_planning_curve`-pad).
2. **Overlay-inactief fast path:** wanneer what-if uit staat of overlay PM-presentatie niet raakt, maximaal **één** `_ltap_preventief_series`-aanroep; display = gefilterde baseline.

Gedrag en vorm van `LCCPlanningCurve` blijven ongewijzigd; som preventief over jaren reconcileert nog steeds met scoped motor-PM-totaal.

## Acceptance criteria

- [ ] Spy/call-count: `reconcile_planning_curve_pm_total` ≤ 1 per curve-build op referentie-fixture.
- [ ] Spy/call-count: inactive overlay → `_ltap_preventief_series` (of equivalent LTAP-build) ≤ 1 per curve-build (was 2).
- [ ] Actieve overlay met wijzigingen behoudt correcte baseline vs what-if totalen (bestaande LCC-planning tests groen).
- [ ] CM-only filter en type-filter-unie gedrag ongewijzigd t.o.v. slice 28/29.

## Blocked by

None.
