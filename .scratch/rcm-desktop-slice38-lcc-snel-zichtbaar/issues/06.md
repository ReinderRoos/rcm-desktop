# Issue 06 — Perf-contract slice 38 + handoff

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice38-lcc-snel-zichtbaar/PRD.md` (slice 38).

## What to build

Uitbreiding `tests/perf/` met **niet-flaky** call-count guardrails voor slice 38 + sessie-handoff:

- `tests/perf/test_slice38_regression.py` met `@pytest.mark.perf`
- `baseline.json` → `slice38_contracts`:
  - `lcc_year_click_curve_rebuilds_max: 0`
  - `lcc_inactive_overlay_ltap_builds_max: 1` (per curve build)
  - `lcc_reconcile_calls_per_curve_max: 1`
  - `post_run_lcc_warmup_builds_max: 1` (indien issue 05 meelevert)
- `KANBAN_HANDOFF.md` in slice-map met handmatige Haarlem-checklist (LCC open, jaarklik 3×, what-if + shift één taak, filter toggle)
- Update `tests/perf/README.md` met slice38-contracts en run-instructie

Geen wall-clock timing in CI.

## Acceptance criteria

- [ ] `pytest -m perf tests/perf/test_slice38_regression.py` groen lokaal.
- [ ] Alle slice 38 contracten in `baseline.json` gedocumenteerd.
- [ ] Bestaande slice 36/37 perf-tests blijven groen.
- [ ] `KANBAN_HANDOFF.md` aanwezig met wat gebouwd is, teststatus en handmatige validatiestappen.
- [ ] Issues 01–05 triage → `done` in handoff (of expliciet open items).

## Blocked by

- Issue 01
- Issue 02
- Issue 03
- Issue 04
- Issue 05

## Notes

- Geen timing-asserties in CI (flaky); alleen call-count / cache-hit gedrag.
- Prior art: `test_slice36_regression.py`, `test_slice37_regression.py`.
