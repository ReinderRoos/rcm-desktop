# Issue 03 — LTAP PM cost series (light path)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice38-lcc-snel-zichtbaar/PRD.md` (slice 38).

## What to build

Nieuwe smalle adapter-API naast `build_ltap_view` voor LCC **plot-build** zonder volledige `LTAPTaskDetail`-objecten:

- Input: zelfde kwargs als LTAP-cache (overlay anchors, disabled set, PBS-scope, type-filters).
- Output: per horizon-index `(raw_pm_eur, filtered_pm_eur)` — **geen** task-detailrijen.

Integreer in curve-builder zodat staafgrafiek + jaartabel via light path gaan. **Jaardetail** blijft `get_ltap_view` → volledige LTAP-view voor PM-takenlijst en what-if-toolbar.

Light path en full view delen `LTAPViewCache` waar overlay/scope identiek is.

## Acceptance criteria

- [ ] Unit test: PM-totalen per jaar uit light path matchen som van `LTAPTaskDetail`-kosten uit full view op fixture (bijv. `one_fm_planning`).
- [ ] Curve-build via light path levert identieke `LCCPlanningCurve`-totalen als huidige full-LTAP pad (parity op referentie-fixture).
- [ ] Jaardetail na jaarklik gebruikt full LTAP-view; geen regressie in detailrijen (label, type, faalwijze, kosten, passief-kolom).
- [ ] LTAP-cache hergebruik: na plot-build + jaarklik geen cold LTAP start wanneer cache-key gelijk (call-count of cache-hit test).

## Blocked by

- Issue 02
