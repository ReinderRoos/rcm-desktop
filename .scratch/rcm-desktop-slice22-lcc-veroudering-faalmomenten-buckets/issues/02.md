# Issue 02 — LCC slice 22 fase 2: SSOT aging-loop + faalmomenten per bucket (CDF-segmenten) + copy + tests (tracer)

**Status:** done  
**Type:** AFK  
**Triage-labels:** done

## Parent

- `../PRD.md` (slice 22 — LCC correctief: **faalmomenten**-buckets voor veroudering, fase 1 + fase 2)

## Acceptance criteria

- [x] **Eén** aging-pad in de kern levert **totaal én per-bucket verwachte faalmomenten**; **geen** duplicate state machine in LCC-orchestratie.
- [x] **Totaal** aging-**verwachte faalmomenten** blijft **regressie-gelijk** met de huidige lifecycle-functie op representatieve cases (incl. randgevallen die al in tests zitten).
- [x] **Som buckets** = dat totaal (zelfde tolerantie als elders); **correctieve EUR** over jaren reconcileert nog steeds met `FMResult`.
- [x] Verdeling per iteratie gebruikt **intervalmassa’s / CDF-stukken**, **niet** volledige `p` op één bucket via alleen conditioneel gemiddelde.
- [x] **Tooltips** reflecteren post-fase‑2 semantiek (geen misleidende fase‑1‑alleen waarschuwing).
- [x] **`CACHE_INPUTS_VERSION`** alleen bij echte motor-/hash-wijziging (normaal niet).

## Blocked by

- `01.md` (fase 1) — afgerond.
