# Issue 06 — Orchestrator + dirty guard + EditingHost per venster + cache-merge

**Triage:** done  
**Type:** AFK  
**Afgerond:** 2026-05-28

## Parent

PRD: `.scratch/rcm-desktop-slice53-werkruimte-architectuur-golf/PRD.md`

## What to build

**Orchestrator:** breid `ResultsWorkspaceController` (of vervangende module) uit zodat **run klaar → presentatie-rebuild → modus-bind** Qt-vrij planbaar is; view voert plan uit (signalen, widgets).

**Dirty guard:** beleid in adapter (pure); view toont alleen dialoog. Verwijder `dirty_session_coordinator` shim.

**EditingHost:** geen process-global `_process_host`; host per `ResultsWorkspaceWindow` (en ValidateWindow waar nodig); tests injecteren host.

**Presentatie-cache:** voeg `presentation_cache_service`, `workspace_presentation_cache`, `presentation_lazy_service` samen waar mogelijk — **bij voorkeur deze PR**; bij te grote diff (>~400 regels netto): orchestrator eerst, cache-merge vervolg-PR binnen 48u.

## Acceptance criteria

- [x] View: geen `_load_presentation_cache` / run-complete orchestratie die adapter-logica dupliceert (`_load_presentation_from_disk` + controller plannen)
- [x] Dirty guard unit-testbaar zonder `views.grid_dirty_guard` import vanuit adapter (`adapter/dirty_guard_policy.py`)
- [x] Twee hosts in één testproces mogelijk (geen gedeelde global in workspace-window/validate-window; test met twee vensters)
- [x] Post-run + LCC warmup regressie groen (`test_slice37_*`, workspace run tests)
- [x] Handoff noteert of cache-merge in deze PR of vervolg-PR zit

## Blocked by

- Issue 05
