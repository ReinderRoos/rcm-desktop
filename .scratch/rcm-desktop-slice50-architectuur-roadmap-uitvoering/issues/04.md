# Issue 04 — Tracer: `commit_fm_edit` façade (editor + grid)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice50-architectuur-roadmap-uitvoering/PRD.md` (fase 2, arch-kandidaat #2).

## What to build

Eén **commit-façade** achter de tabulaire editing-pipeline: validate → materialize → optioneel save → incrementele run — gedeeld door **faalwijze-editor** en **faalwijzen-grid** materialize/run.

**Interface (besluit uit PRD):**

```text
commit_fm_edit(
  session: EditingSession,
  bundle: FmEditBundle,
  *,
  path: Path | None,
  save_to_disk: bool,
  run_policy: RunPolicy,  # blocking | RunRunner callback
) -> FmEditCommitResult
```

**Gedrag**

- `replace_fm_scope` / pipeline-stappen zitten **binnen** de façade, niet verspreid in views.
- Editor **OK** en grid save/run roepen dezelfde façade aan (RunRunner waar UI async vereist — voortbouwen op slice 46-07).
- Views: draft/widgets alleen; geen handmatige `session["edit_current"]` patches voor consistency (blijft read-only **FmEditConsistencyService** op rijen).
- **FM-detail behouden** na editor-commit (slice 49) blijft werken.

**Tests**

- Unit: happy path + validatiefout; patch `rcm_core.incremental_run` waar run nodig is.
- pytest-qt smoke: editor OK op fixture; grid materialize triggert zelfde orchestration-entry (smoke-niveau).

## Acceptance criteria

- [x] `commit_fm_edit` bestaat; editor en grid gebruiken deze façade voor commit/run.
- [x] Geen duplicate validate→save→run-keten in views voor FM-edit (editor; werkruimte grid-sluiten).
- [x] Unit tests façade groen; bestaande fm_edit commit tests groen of gemigreerd.
- [x] `test_slice49_fm_editor_ux` en FM-detail workspace-test (indien aanwezig) groen.
- [x] Incrementele run default `full_recompute=False` ongewijzigd t.o.v. huidig editor-gedrag.

## Blocked by

- Slice 50 issue **03** (`load_fm_edit_scope` done).
