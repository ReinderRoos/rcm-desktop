# Issue 03 — Tracer: unified `load_fm_edit_scope` (project | EditingSession)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice50-architectuur-roadmap-uitvoering/PRD.md` (fase 2, arch-kandidaat #1).

## What to build

Eén **diepe adapter-module** voor FM-edit scope: laad **FmEditBundle** voor één `fm_id` uit óf een geladen **RCMProject** óf een **EditingSession** — één graph-walk, geen dubbele loaders.

**Interface (besluit uit PRD):**

```text
load_fm_edit_scope(
  source: RCMProject | EditingSession,
  fm_id: str,
) -> FmEditBundle
```

**Gedrag**

- Zelfde bundle-inhoud (faalwijze, PBS, fm/pm links, pm tasks, task_groups, effect_klassen in scope) ongeacht bron wanneer data equivalent is.
- Bestaande `load_bundle` / `load_bundle_from_session` migreren naar deze seam; publieke callers (faalwijze-editor, commit) gebruiken alleen `load_fm_edit_scope`.
- Oude entrypoints deprecated verwijderen of dunne wrapper met deprecation — geen tweede walk.

**Tests**

- Unit: project vs. sessie equivalence op fixture (zelfde `fm_id`, sessie gevuld vanuit project).
- Bestaande bundle-scope tests blijven groen na migratie.

**Niet in deze issue:** commit-façade (issue 04); EditingHost (slice 46-08/09).

## Acceptance criteria

- [x] `load_fm_edit_scope` is de canonieke loader; geen parallelle graph-walk in productiecode.
- [x] Faalwijze-editor en commit-pad laden bundle via deze seam.
- [x] Unit equivalence test project | sessie groen.
- [x] Bestaande `test_desktop_fm_edit_services` (bundle/scope) groen.
- [x] Geen Qt in de loader-module.

## Blocked by

- Slice **46 issue 09** (`done`) — gedeelde sessie + workspace grid-host + registry weg.
