# Issue 02 — Scope-filter leaf-dekking (paneel)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice55-meekoppel-discovery-parent-rollup/PRD.md`

## What to build

**Paneel-scope (`meekoppel_panel_service`):**

- `_filter_groups_by_scope`: toon `MeekoppelLocationGroup` als **minstens één** `task.pbs_id` (leaf) in `covered` valt — niet alleen `row.pbs_id in covered`.
- Zelfde leaf-dekking voor filter op `scope_id` (subboom): parent-rij zichtbaar als een groeps-REV-leaf onder die subboom valt.
- PBS-multi-select preview/apply (`collect_rev_tasks_for_pbs_selection`) **niet** wijzigen.

Optioneel: copy empty-state onderscheid selectie vs venster (zie PRD Further Notes).

## Acceptance criteria

- [ ] Panel/discovery integratietest: multi-select op **child** PBS toont parent-groep wanneer groeps-REV op die child valt.
- [ ] Test: parent-groep **niet** zichtbaar als geen groeps-leaf onder selectie valt.
- [ ] Regressie: zonder PBS-selectie gedrag ongewijzigd t.o.v. issue 01 discovery-output.
- [ ] Locatierij preview/apply gebruikt nog steeds `group.tasks` van discovery (geen aparte leaf-bucket).

## Blocked by

Issue 01.
