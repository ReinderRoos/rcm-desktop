# Issue 01 — `bundling_pbs_id` + discovery parent-rollup

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice55-meekoppel-discovery-parent-rollup/PRD.md`

## What to build

**Diepe helper (Qt-vrij):**

- `bundling_pbs_id(project, fm_pbs_id) -> str`: parent indien aanwezig in `pbs_items`, anders leaf.

**Discovery refactor:**

- Bucket REV-taken op `bundling_pbs_id`, niet op raw `fm.pbs_id`.
- `MeekoppelRevTask.pbs_id` blijft **leaf** (`fm.pbs_id`).
- `MeekoppelLocationGroup.pbs_id` en `path_label` op **bundelsleutel** (parent of fallback).
- Groepsregels ongewijzigd: ≥ 2 REV, span ≤ `window_years`.

Geen paneel-scope of ADR in dit issue.

## Acceptance criteria

- [ ] Unit tests: twee sibling-leaves onder één parent → één groep met parent-`pbs_id`, twee taken met verschillende leaf-`pbs_id`.
- [ ] Unit tests: REV op verschillende parents → aparte groepen.
- [ ] Unit tests: ontbrekende/orphan parent → fallback leaf per taak.
- [ ] Unit tests: span > venster → geen groep; ≥ 2 REV span ≤ venster → groep op parent-sleutel.
- [ ] Bestaande discovery-tests aangepast of uitgebreid; geen Qt-imports in gewijzigde adapter-modules.

## Blocked by

None — can start immediately.
