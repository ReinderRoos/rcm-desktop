# Issue 02 — Smoke: kern werkt los via CLI op awzi-fixture

**Status:** ready-for-agent

## Parent

[01](01.md)

## What to build

Bewijs dat de geporteerde kern werkt zonder Qt aan te raken. Output van
`validate` en `run --full` op `tests/fixtures/awzi_haarlem_waarderpolder_demo.rcm.json`
moet plausibel zijn (geen tracebacks, exit 0, sane PBS-totals).

## Acceptance criteria

- [ ] `python -m rcm_core.cli validate tests/fixtures/awzi_haarlem_waarderpolder_demo.rcm.json` geeft exit 0.
- [ ] `python -m rcm_core.cli run tests/fixtures/awzi_haarlem_waarderpolder_demo.rcm.json --full` geeft exit 0 en
      toont een tabel met PBS-resultaten.
- [ ] `python -m pytest` groen (volledige gepoorte suite + lege adapter-suite).
- [ ] Eventuele import- of namespace-bugs uit de port worden hier opgelost
      voordat issue 03 start.

## Blocked by

None.

## Comments
