# Issue 01 — plan_nb_filter_refresh pure functie + binding-update

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice92-workspace-derived-refresh/PRD.md` (slice 92).

## What to build

1. Nieuwe module `rcm_desktop/adapter/workspace_derived_refresh.py` met:
   - `plan_nb_filter_refresh(project, fm_results) -> NbEffectFilterPresentation`
   - Pure functie, geen Qt.

2. Update `rcm_desktop/views/panels/top10_workspace_binding.py`:
   - `refresh_nb_effect_filter_combo` delegeert berekening aan `plan_nb_filter_refresh`.
   - Binding haalt zelf `fm_results` op uit `window._project_session()`.

3. Tests in `tests/test_slice92_workspace_derived_refresh.py`:
   - `plan_nb_filter_refresh(None, ())` → lege presentatie
   - `plan_nb_filter_refresh(project, ())` → entries aanwezig, geen selecteerbare
   - Bestaande slice 91 tests blijven groen

## Acceptance criteria

- [x] `plan_nb_filter_refresh` is Qt-vrij aanroepbaar.
- [x] `refresh_nb_effect_filter_combo` gebruikt `plan_nb_filter_refresh`.
- [x] Tests slagen zonder Qt.
- [x] Slice 91 NB-filter-test blijft groen.

## Blocked by

None.
