**Triage:** ready-for-agent

## Parent
PRD: `.scratch/rcm-desktop-slice95-strategic-roadmap-juni2026/PRD.md` (slice 95 strategic roadmap).

## What to build
Breid `WorkspaceViewEntry` uit met een declaratief **chrome-profiel** (`WorkspaceChromeProfile`): flags `allows_new_fm`, `shows_nb_effect_filter`, `shows_metric_combo`, `shows_batch_faalwijzen`, `shows_column_crop`, en `toolbar_family` (`none` | `top10` | `lcc` | `fm` | `input_grid`). Vul profielen per view in de registry. Migreer slice 93-gedrag (Nieuwe faalwijze alleen op `input.faalwijzen`) naar metadata. `ResultsWorkspaceOrchestrator` leest registry i.p.v. hardcoded view-checks in `_plan_fm_toolbar` en verwante chrome-planners.

## Acceptance criteria
- [ ] `WorkspaceChromeProfile` en chrome per `view_id` in `WORKSPACE_VIEW_REGISTRY`
- [ ] Orchestrator `plan_ui_sync` deriveert FM-toolbar en gerelateerde chrome uit registry
- [ ] `new_fm_visible` true op `input.faalwijzen`, false op `output.fm_results` (slice 93-pariteit)
- [ ] Tests in `tests/test_slice95_chrome_registry.py` groen

## Blocked by
None - can start immediately

## User stories covered
5, 6, 7, 8
