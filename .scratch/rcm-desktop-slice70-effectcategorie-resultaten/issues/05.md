# Issue 05 — Metric Effectimpact per effectcategorie (hybride Top 10)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice70-effectcategorie-resultaten/PRD.md` (slice 70, issue 05).  
GitHub: *(na publicatie — zie `GITHUB_PUBLISH.md`)*

## What to build

Hybride metric-gedrag (spike besluit 5):

| Bron | Default metric |
|------|----------------|
| Component / Faalwijze | Niet-beschikbaarheid (totaal) — ongewijzigd |
| Effectklasse | **Effectimpact** — auto-switch bij bronwissel |

Bij bron Effectklasse: rijen per effectklasse; eenheid/label per `categorie` (beschikbaarheid → uren; veiligheid → incidenten). VGM heet **niet** "Niet-beschikbaarheid".

- Filter op `EffectKlasse.categorie` (taxonomy issue 11; stub allowed tot 11 done)
- Horizon/presentatie via `EffectImpactService` + `ContributionPresentation`

## Acceptance criteria

- [ ] Golden cause: 4 effectrijen met RF 0,1 / 0,9 / 1,0 / 0,5 en categorie-correcte eenheden.
- [ ] NB totaal op Component-bron: geen regressie t.o.v. baseline tests.
- [ ] `contribution_horizon_value_service` delegeert NB-per-effect naar service (geen duplicate math).
- [ ] Unit tests per horizon-modus (lifecycle + één kalenderjaar).

## Blocked by

Issue 03, Issue 04
