## Parent

PARENT_ISSUE_URL

## What to build

Implementeer spike-besluit issue 01 (optie A — split raw):

- Nieuw op `FMResult`: `fm_effect_bijdragen` (incidenten-equivalent: `expected_failures × RF`) en `pm_effect_bijdragen` (uren: `duration × RF × executions`)
- Zelfde split voor `effect_bijdragen_per_jaar` buckets waar van toepassing
- `effect_bijdragen` blijft als deprecated mixed som voor backward-compat; UI gebruikt split
- Bump `CACHE_INPUTS_VERSION` wanneer serialisatie/FM-hash nieuwe velden bevat
- Tolerante `from_dict` voor bestaande `.rcm.json` runs

Geen normalisatie naar uren in de motor — categorie-formules horen in `EffectImpactService` (issue 03).

## Acceptance criteria

- [ ] Golden cause `06H-350.1.1.1.1.1.A.1`: vier effectrijen met verwachte RF/waarden na split
- [ ] `test_effect_per_jaar.py` blijft groen (bucket-reconciliatie)
- [ ] `from_dict` backward-compat voor legacy runs
- [ ] Eenheden gedocumenteerd op modelvelden

## Blocked by

CHILD_01_ISSUE_URL (semantics spike — gate cleared)
