# Issue 02 — Motor: CM/PM split effectbijdragen

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice70-effectcategorie-resultaten/PRD.md` (slice 70, issue 02).  
GitHub: *(na publicatie — zie `GITHUB_PUBLISH.md`)*

## What to build

Implementeer spike-besluit issue 01 (optie A — split raw):

- Splits `fm_effect_bijdragen` / `pm_effect_bijdragen` op `FMResult` (CM = incidenten-equivalent; PM = uren)
- Zelfde split voor `effect_bijdragen_per_jaar` buckets waar van toepassing
- `effect_bijdragen` blijft deprecated mixed som voor backward-compat
- Bump `CACHE_INPUTS_VERSION` indien serialisatie/hash wijzigt

Geen normalisatie naar uren in de motor — categorie-formules in `EffectImpactService` (issue 03).

## Acceptance criteria

- [ ] Golden cause: 4 effectrijen met verwachte RF/waarden na normalisatie.
- [ ] `test_effect_per_jaar.py` blijft groen (reconciliatie lifecycle vs buckets).
- [ ] `from_dict` backward-compat voor bestaande `.rcm.json` runs.
- [ ] Spike-keuze expliciet in code comments / model docstrings.

## Blocked by

Issue 01
