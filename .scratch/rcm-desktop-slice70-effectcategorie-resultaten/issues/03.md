# Issue 03 — Deep module: EffectImpactService

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice70-effectcategorie-resultaten/PRD.md` (slice 70, issue 03).

## What to build

Qt-vrije deep module (kern of adapter) met:

```
aggregate(project, fm_results, *, scope_id, metric, presentation, categorie_filter?)
  → tuple[EffectImpactRow, ...]
```

`EffectImpactRow`: klasse_id, label, categorie, rf_display, waarde_cm, waarde_pm, waarde_totaal, eenheid, share_pct.

SSOT helper: `collect_fm_ids_for_functie(project, functie_id, kind)`.

Geen duplicatie in chart/rapport/verification services — die delegeren hiernaartoe.

## Acceptance criteria

- [ ] Unit tests: golden cause 4 rijen; scope filter; categorie filter; share_pct som ≈ 100%.
- [ ] CM vs PM split per effectklasse.
- [ ] Geen Qt-imports in module.
- [ ] `test_architecture_deepening.py` of sibling gate: module importable from adapter without view.

## Blocked by

Issue 02
