# Issue 10 — AW-pariteit: EffectCost (informatief)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice70-effectcategorie-resultaten/PRD.md` (slice 70, issue 10).

## What to build

- Uitbreiding `rcm_cost_benchmark` of sibling `effect_cost_parity` (Qt-vrij).
- RCM2: waar `cost_gevolg_eur` > 0 → vergelijk `Σ(effect × cost)`; anders incidenten/uren-only weergave.
- AW: `EffectCost` + err-kolommen uit `import_settings.isograph_causes` — **informatief**, geen gate v1.
- Modelcontrole-dialog: tab of sectie "Effecten" (read-only).

## Acceptance criteria

- [ ] Unit tests: FM met AW EffectCost → verdict informatief/pass binnen band waar err aanwezig.
- [ ] FM zonder benchmark → "geen data", geen fail.
- [ ] ADR-0008 further notes verwijzing (documentatie).
- [ ] Geen wijziging bestaande TotalCost/TotalTdt gates.

## Blocked by

Issue 03, Issue 01
