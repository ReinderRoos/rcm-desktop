## Parent

PARENT_ISSUE_URL

## What to build

Import effect-taxonomie (HITL done — spike §4–5):

- Map AW `RcmEffects.Type` → genormaliseerde `EffectKlasse.categorie` (`beschikbaarheid`, `veiligheid`, `kosten`, `overig`)
- Nieuw veld `EffectKlasse.aw_effect_type` voor ruwe AW `Type` (traceerbaarheid)
- Vervang directe pass-through Type → categorie

Mapping v1:

| AW Type | categorie |
|---------|-----------|
| Schutten, Keren, Kruisen, Spuien | beschikbaarheid |
| VGM | veiligheid |
| leeg / onbekend | overig + waarschuwing |

Geen `CostPerOccurrence`-import (ADR-0004). Geen wijziging slice 69 link-logica.

## Acceptance criteria

- [ ] CM-fixture: effectklassen hebben consistente categorie strings
- [ ] Unit tests mapping tabel (VGM → veiligheid, Schutten → beschikbaarheid, …)
- [ ] Onbekend type → overig + import-waarschuwing
- [ ] Import regressie slice 69 groen

## Blocked by

CHILD_01_ISSUE_URL (semantics spike — gate cleared)
