# Issue 02 — Tracer: must-kolommen C + celbewerking in grid

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice46-fm-edit-fase2/PRD.md` (slice 46).

## What to build

Breid het batch-grid **end-to-end** uit met de overige **must (C)**-velden uit de PRD, plus **should (B)** waar haalbaar in dezelfde sprint; anders documenteer fallback naar **could (A)** in de issue-notities.

**Velden (must C)**

- `mttf_jaar`, `sigma_jaar`, `faalwijze_omschrijving`, `functie_id`, `repair_quality` — bewerkbaar met delegates (float/FK/combo/tekst).
- Validatieregels: o.a. sigma/MTTF consistent met `failure_type` (aging vs random).

**Velden (should B)**

- `cost_cm_eur`, `p_ongewenste_gebeurtenis` — behouden/integreren in schema-gedreven kolomset indien C compleet is.

**Adapter + UI**

- Zelfde schema-allowlist-patroon als issue 01; enkelvoudige celwijziging via bestaande `apply_change`.
- Bulk (selectie) uit issue 01 uitbreiden naar alle nieuwe kolommen.
- Nog steeds embedded in ValidateWindow (extract volgt in issue 04).

**Tests**

- Unit: celwijziging + validatiefout per veld; bulk op minstens één extra veld (bijv. `mttf_jaar`).
- `functie_id` FK-validatie geweigerd bij ongeldige waarde.

## Acceptance criteria

- [ ] Must-set C-kolommen zijn zichtbaar en bewerkbaar in het grid.
- [ ] Should-set B-kolommen werken, of fallback A (alleen type+NMF+MTTF+sigma) is expliciet genoteerd in PRD/issue + minstens type+NMF+MTTF+sigma blijven groen.
- [ ] Geen `bouwjaar`, effectlinks, PM of CM-split in het grid.
- [ ] Bulk op selectie werkt voor minstens drie verschillende velden (incl. type/NMF uit 01).
- [ ] Unit tests voor validatie en bulk op nieuw veld.

## Blocked by

- Issue 01 (`.scratch/rcm-desktop-slice46-fm-edit-fase2/issues/01.md`)
