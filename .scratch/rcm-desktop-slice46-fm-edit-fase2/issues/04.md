# Issue 04 — Tracer: ValidateFaalwijzenPanel (host-neutraal) + dunne shell

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice46-fm-edit-fase2/PRD.md` (slice 46 — milestone 46 afronden).

## What to build

Verplaats het **complete** faalwijzen-batch-grid (kolommen 01–02, filters/bulk 03) naar een **host-neutraal** panel; ValidateWindow wordt een dunne shell zonder compare/LTAP in dezelfde refactor.

**Panel**

- Widget bevat tabel, snelfilters, zoek, bulk-toolbar/contextmenu.
- Constructor/factory: `FaalwijzenEditService` + project-ref; signals voor dirty (indien nodig).
- Geen compare-, LTAP-, run- of save-orchestratie in het panel.

**ValidateWindow**

- Embedt panel; behoudt bestaande panel-toggle, settings persistence, run/save/analyse-knoppen en dirty-state op service-niveau.
- Geen regressie in preview/LTAP/compare-panelen (ADR-0006).

**Tests**

- pytest-qt smoke: panel mount op fixture; filter + één bulk-actie; dirty signal bereikt shell.

## Acceptance criteria

- [ ] Faalwijzen-grid UI leeft in host-neutraal panel, niet inline in ValidateWindow.
- [ ] Alle acceptatie uit issues 01–03 blijft groen na extract.
- [ ] Panel importeert geen `rcm_core` (typing-only OK).
- [ ] ValidateWindow run/save/analyse gedrag ongewijzigd voor de analist.
- [ ] pytest-qt smoke: panel + filter + bulk.

## Blocked by

- Issue 03 (`.scratch/rcm-desktop-slice46-fm-edit-fase2/issues/03.md`)
