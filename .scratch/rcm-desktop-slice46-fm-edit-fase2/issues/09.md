# Issue 09 — Tracer: workspace grid-host + optioneel `time_duration` coercion

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice46-fm-edit-fase2/PRD.md` (slice 48 — afronding).

## What to build

**End-to-end** optionele uitbreidingen na gedeelde sessie: batch-grid in de **resultatenwerkruimte** en optionele registry-coercion voor nested tijd.

**Workspace grid-host (should)**

- Hergebruik `ValidateFaalwijzenPanel` in resultatenwerkruimte (menu, dock of modus-specifieke entry — kies minimaal één zichtbare entry).
- Zelfde service/sessie als Validate-host via issue 08; geen compare/scenario UI (ADR-0006).

**Registry (could)**

- `time_duration` coercion in **Editing schema registry** alleen als `test_editing_schemas_parity` + validatietests volledig groen; anders dict-vorm behouden en in CONTEXT.md noteren.

**Documentatie**

- `CONTEXT.md`: drie lagen (batch-grid vs modale editor vs verificatie/compare).
- `ARCHITECTURE_DEFERRED.md` blijft verwijzen naar deze PRD als afgerond.

**Tests**

- pytest-qt smoke: panel open in werkruimte, één celwijziging + filter.
- Parity tests groen indien `time_duration` wordt toegevoegd.

## Acceptance criteria

- [x] Analist kan batch-grid vanuit resultatenwerkruimte openen (entrypoint: knop `WORKSPACE_MENU_FAALWIJZEN_BATCH` in `ResultsWorkspaceWindow`).
- [x] Grid in werkruimte gebruikt gedeelde sessie uit issue 08 (via `EditingHost`).
- [x] `time_duration` coercion niet gemerged; dict-vorm behouden en expliciet in `CONTEXT.md`.
- [x] `CONTEXT.md` bijgewerkt met grid/editor/validate-rollen.
- [x] pytest-qt smoke werkruimte-host groen (`test_workspace_batch_grid_smoke_filter_and_edit`).

## Implemented (2026-05-27)

- Verwijderd: `rcm_desktop/adapter/faalwijzen_grid_registry.py` (registry weg).
- `ValidateWindow` en `ResultsWorkspaceWindow` praten nu direct met `EditingHost`.
- Nieuwe smoke test in werkruimte: panel open + filter + celwijziging.

## Blocked by

- Issue 08 (`.scratch/rcm-desktop-slice46-fm-edit-fase2/issues/08.md`)
