# Issue 05 — Tracer: `replace_fm_scope` + downtime dict + editor correctief

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice46-fm-edit-fase2/PRD.md` (slice 47 — milestone 47 start).

## What to build

**End-to-end** versterking van FM-editor commit-pad: atomische FM-scope in de sessie, registry-compleet voor downtime, gedeelde mappers — zichtbaar in de **Correctief-tab** (hersteltijd) zonder view-duplicatie.

**Kern**

- `EditingSession.replace_fm_scope(bundle)` vervangt handmatige multi-entity splice; gedrag equivalent aan huidige `apply_bundle_scope`.
- `downtime_per_failure` in `ENTITY_SCHEMAS["faalwijzes"].field_types` als **dict** `{value, unit}` in edit-buffer.
- Adapter **row-mappers**: uren ↔ dict; gebruikt door FmEditorDialog correctief-tab (geen inline dict-logica in view).
- `FmEditCommitService` roept `replace_fm_scope` aan i.p.v. losse splice.

**Tests**

- Unit: `replace_fm_scope` equivalentie met bestaande bundle-scope tests.
- `test_editing_schemas_parity` groen.
- Unit: mapper round-trip; editor commit met gewijzigde downtime → project reflecteert dict-vorm.

Geen `time_duration` registry-type (issue 09). Geen RunRunner (issue 07).

## Acceptance criteria

- [ ] `replace_fm_scope` is de canonieke FM-scope apply; tests groen.
- [ ] `downtime_per_failure` staat in de registry; parity groen.
- [ ] Editor correctief-tab gebruikt adapter-mappers; OK-commit slaat downtime correct op.
- [ ] Geen duplicate downtime-mapping in views.
- [ ] Incrementele run na editor-OK blijft werken (bestaand gedrag).

## Blocked by

None — can start immediately (parallel met milestone 46 indien andere agent op 01–04).

**Aanbevolen volgorde:** na issue 04 voor minder merge-conflict in ValidateWindow.
