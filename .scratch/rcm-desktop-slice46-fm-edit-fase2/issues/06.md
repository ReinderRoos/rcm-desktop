# Issue 06 — Tracer: FmEditBundleAssembler (view → adapter)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice46-fm-edit-fase2/PRD.md` (slice 47).

## What to build

**End-to-end** editor-commit zonder bundle-assemblage in de view: widgets/state → **FmEditBundle** in de adapter, inclusief CM-split en downtime-dict via issue-05-mappers.

**Adapter**

- `FmEditBundleAssembler` (of equivalente deep module): bouwt bundle uit editor-state DTOs; hergebruikt `CmCostSplitMapper` en downtime-mappers.

**View**

- `FmEditorDialog` roept alleen assembler + `FmEditCommitService` aan; geen `_collect_bundle`-achtige domeinlogica meer in de view.

**Tests**

- Unit: assembler round-trip voor fixture-FM (faalwijze, PBS, CM-split, downtime, minstens één effectlink-placeholder indien aanwezig in state).
- Bestaande editor commit-tests blijven groen met assembler-pad.

Geen RunRunner (issue 07). Geen dirty guard (issue 07).

## Acceptance criteria

- [ ] Bundle-assemblage leeft in adapter; view bevat geen entity-splice of CM/downtime-domeinlogica.
- [ ] OK in editor commit nog steeds: validate → save → incrementele run (sync pad mag nog).
- [ ] Unit round-trip assembler test groen.
- [ ] Geen Qt in assembler-module.

## Blocked by

- Issue 05 (`.scratch/rcm-desktop-slice46-fm-edit-fase2/issues/05.md`)
