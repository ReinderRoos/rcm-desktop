# Issue 03 — Tracer: snelfilters, zoek + bulk op alle zichtbare rijen

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice46-fm-edit-fase2/PRD.md` (slice 46).

## What to build

**End-to-end batch-herstel-UX** bovenop de kolommen uit issues 01–02: analist filtert homogene modelfouten en past wijzigingen toe op **alle zichtbare (gefilterde)** rijen zonder handmatige multi-select.

**UI**

- Snelfilters boven het grid: `failure_type`, NMF (`is_evident`).
- Zoekveld op `fm_id` en `faalwijze_omschrijving`.
- `QSortFilterProxyModel` (of equivalent) zodat bulk/filter op **zichtbare** rijen werkt; sorteren toegestaan.
- Bulk-acties: “pas toe op selectie” (01/02) + **“pas toe op alle zichtbare”**.
- Duidelijke foutmeldingen bij bulk-validatiefout (per fm_id/veld); optionele waarschuwing bij >N rijen (N in issue/PRD vastleggen, bijv. 500).

**Adapter**

- Bulk op zichtbare rijen roept `apply_bulk_change` aan met fm_id-lijst uit proxy; geen apart validatiepad.

**Tests**

- Unit/integration: filter “alleen exponentieel” → zichtbare set → bulk `failure_type` → alleen gefilterde FM’s gewijzigd.
- Test: bulk op zichtbare geblokkeerd als één rij invalid (all-or-nothing).

## Acceptance criteria

- [ ] Snelfilters failure_type en NMF werken en beperken zichtbare rijen.
- [ ] Zoek op fm_id en omschrijving verfijnt binnen gefilterde set.
- [ ] “Pas toe op alle zichtbare” past bulk toe via dezelfde validatie als selectie-bulk.
- [ ] Na filter+wijziging blijven niet-zichtbare rijen ongewijzigd (test bewijst dit).
- [ ] Validatiefouten na bulk zijn zichtbaar/gerapporteerd (geen stille skip).
- [ ] pytest-qt smoke optioneel maar aanbevolen: filter + bulk op fixture-project.

## Blocked by

- Issue 02 (`.scratch/rcm-desktop-slice46-fm-edit-fase2/issues/02.md`)
