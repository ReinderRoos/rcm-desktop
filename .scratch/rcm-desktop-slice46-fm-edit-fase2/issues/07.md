# Issue 07 — Tracer: editor via RunRunner + dirty guard grid ↔ editor

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice46-fm-edit-fase2/PRD.md` (slice 47 — milestone 47 afronden).

## What to build

**End-to-end** veilige en responsieve editor in de resultatenwerkruimte, met conflictbescherming tegen onopgeslagen grid-wijzigingen in ValidateWindow.

**RunRunner**

- FmEditorDialog OK-pad: commit + incrementele run via **RunRunner** (voortgang; annuleren waar ondersteund).
- Resultaat (`FmEditCommitResult`) terug naar werkruimte zoals nu (presentatie-cache/hydrate patroon behouden).

**DirtySessionCoordinator**

- Bij openen editor (werkruimte dubbelklik): als `FaalwijzenEditService.is_dirty()` → modal: grid **opslaan**, grid **verwerpen**, of **editor annuleren**.
- Zelfde guard wanneer editor vanuit Validate wordt geopend (indien entry bestaat of wordt toegevoegd).

**Tests**

- pytest-qt: dirty guard toont modal en annuleren opent geen editor.
- Gepatchte `incremental_run`: RunRunner-pad triggert run voor hash-gewijzigde FM’s.

Geen gedeelde sessie (issue 08) — editor mag nog geïsoleerde sessie gebruiken; alleen dirty-check op grid-service.

## Acceptance criteria

- [ ] Editor OK gebruikt RunRunner; UI bevriest niet op groot project (smoke).
- [ ] Incrementele run-gedrag ongewijzigd (geen full recompute default).
- [ ] Dirty guard bij grid-dirty + editor-open op minstens werkruimte-pad.
- [ ] Annuleren/verwerpen/opslaan keuzes gedocumenteerd in gedrag (tests voor annuleren-pad).
- [ ] pytest-qt smoke RunRunner + dirty guard groen.

## Blocked by

- Issue 06 (`.scratch/rcm-desktop-slice46-fm-edit-fase2/issues/06.md`)
