# Issue 08 — Tracer: gedeelde EditingSession + run-orchestratie grid/editor



**Triage:** done  

**Type:** AFK



## Parent



PRD: `.scratch/rcm-desktop-slice46-fm-edit-fase2/PRD.md` (slice 48).



## What to build



**End-to-end** één project-buffer voor batch-grid en FM-editor, met gedeelde run-orchestratie na save/commit.



**Sessie**



- `FaalwijzenEditService` en FM-editor gebruiken dezelfde project-`EditingSession` (geen geïsoleerde editor-clone voor `faalwijzes`-velden).

- Editor opent FM-scope **view** op gedeelde sessie (`replace_fm_scope` / load bundle uit gedeelde buffer).



**Run**



- Grid save/run en editor commit via dezelfde adapter **run-orchestratie**-entry (`RunPolicy` / RunRunner waar UI async nodig).

- Presentatie-cache en foutafhandeling gelijk aan werkruimte-patronen.



**Tests**



- Integration: wijzig `failure_type` in grid → open editor voorzelfde `fm_id` →zelfde waarde zichtbaar zonder stale baseline.

- Smoke: grid save triggert incrementele run via gedeelde orchestration (gepatchte `incremental_run`).



Geen workspace grid-host (issue 09). Geen `time_duration` coercion (issue 09).



## Acceptance criteria



- [x] Grid-wijziging is zichtbaar in editor voor dezelfde FM zonder tussenliggende save naar disk-only workaround.

- [x] Editor commit en grid materialize/run delen orchestration-entry (`commit_fm_edit` via `EditingHost.commit_grid_edits`).

- [x] Integration test grid → editor waarde-gelijkheid groen (`tests/test_editing_host.py`).

- [x] Dirty guard: Qt in `views/grid_dirty_guard.py`; bij gedeelde sessie toont editor grid-wijzigingen direct — guard blijft voor expliciet opslaan/verwerpen vóór editor (minder risico op onbedoeld weggooien).



## Blocked by



- Issue 04 (`.scratch/rcm-desktop-slice46-fm-edit-fase2/issues/04.md`)

- Issue 07 (`.scratch/rcm-desktop-slice46-fm-edit-fase2/issues/07.md`)



## Implemented (2026-05-26)



- `rcm_desktop/adapter/editing_host.py` — `EditingHost`, `get_editing_host()`, `commit_grid_edits` → `commit_fm_edit`.

- `faalwijzen_grid_registry` delegeert naar host (registry verwijderen in **46-09**).

- `views/grid_dirty_guard.py` — Qt dirty-dialog; adapter `dirty_session_coordinator` is dunne re-export.

- Werkruimte: gedeelde sessie naar `FmEditorDialog`; grid-sluiting + dirty-save via `commit_fm_edit`.

