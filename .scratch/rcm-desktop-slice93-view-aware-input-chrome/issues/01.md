# Issue 01 — Orchestrator view-aware FM chrome

**Triage:** done

## Acceptance

- [x] `new_fm_visible` / `batch_faalwijzen_visible` alleen op `input.faalwijzen`
- [x] `column_crop_visible` alleen op `output.fm_results`
- [x] `plan_ui_sync` gebruikt `active_view_id`, niet alleen `modus == fm_detail`
