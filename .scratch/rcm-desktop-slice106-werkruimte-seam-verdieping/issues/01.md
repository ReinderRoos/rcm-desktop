# Issue 01 — Navigatie: `active_view_id` canoniek

**Type:** AFK  
**Triage:** `done`  
**Outcome:** Sidebar FM-editor shell; sticky-aware set_modus; detail_stack_key_for_view; test_slice106_view_id_canonical.py

## Parent

PRD: `.scratch/rcm-desktop-slice106-werkruimte-seam-verdieping/PRD.md` (tranche **106-A**)

## What to build

Maak **`active_view_id`** de enige navigatiesleutel in de resultatenwerkruimte-state.
Leid legacy **`modus`** af uit de view-registry; key detail-pagina's op `view_id` (of
registry page key) zodat LTAP en LCC-plot presets niet per ongeluk state delen.

- `set_active_view(view_id)` wordt het canonieke mutatie-pad voor dropdown, Beeld-menu, sneltoetsen
- `set_modus` wordt compat-shim die naar `set_active_view` routeert
- Orchestrator en venster lezen primair `active_view_id`; modus alleen waar ADR-0012 preset branches het vereisen
- Statische gate: geen nieuwe `set_modus(` call sites buiten state

Start: `/tdd slice 106 issue 01`

## Acceptance criteria

- [ ] Sticky view per zijde (Input/Output) ongewijzigd voor analist
- [ ] LTAP (`output.ltap`) en LCC-plot (`output.lcc_plot`) navigeren correct met eigen preset
- [ ] `test_desktop_results_workspace_state` + slice 79 navigatie-tests groen
- [ ] Nieuwe/uitgebreide tests voor view_id-canoniciteit zonder Qt

## Blocked by

None

## User stories covered

7–14, 40–41
