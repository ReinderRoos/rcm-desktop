# Issue 02 — Faalwijze presentation bundle in RenderPlan

**Type:** AFK  
**Triage:** `done`  
**Outcome:** `FaalwijzePresentationBundle` op RenderPlan; views zonder `build_faalwijze_*`; `test_slice106_faalwijze_bundle.py`.

## Parent

PRD: `.scratch/rcm-desktop-slice106-werkruimte-seam-verdieping/PRD.md` (tranche **106-B**)

## What to build

Verplaats alle Faalwijze-presentatiebouw naar de adapter. **`plan_render`** levert
een unified **`FaalwijzePresentationBundle`** op `RenderPlan` voor FM single-run en
compare. **`fm_detail_workspace_binding`** mapt alleen bundle → table models / charts;
geen `build_faalwijze_*` calls meer in views.

- Single-run en compare delen dezelfde bundle-structuur
- Geen fallback-rebuild in binding bij `None`
- MC P10/P50/P90 en horizon-gedrag (slice 103) behouden
- Statische gate: geen `build_faalwijze_*` import in `rcm_desktop/views/`

Start: `/tdd slice 106 issue 02`

## Acceptance criteria

- [ ] `test_results_workspace_orchestrator` dekt FM-bundle voor single-run en compare
- [ ] Slice 104/105 FM-presentatie tests groen
- [ ] Binding test bevestigt delegatie-only (geen presentatie-build)
- [ ] HILT103 FM single-run + compare smoke (handmatig of bestaande test fixture)

## Blocked by

Issue 01 (aanbevolen; minimale view_id-routing moet stabiel zijn)

## User stories covered

15–23, 40, 42
