# Issue 02 — Haarlem fixture flip aging-heavy (F1 + G2 + G3)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice42-lcc-cm-year-correctness/PRD.md` (slice 42).

## What to build

**In-place mutatie** van het Haarlem-demo-project (`awzi_haarlem_waarderpolder_demo.rcm.json`):

Voor elke faalwijze met `failure_type == "random"` (58 stuks):

```
failure_type      ← "aging"
sigma_jaar        ← round(0.15 * mttf_jaar, 1)
aanname_faalmodel ← aging-formulering (normaalverdeling, σ=15% MTTF); sluit aan bij bestaande aging-FM's in fixture
notes             ← korte verouderingsbeschrijving / demo-notitie
```

- **`repair_quality`**, `cost_cm_eur`, `mttf_jaar`, PM-koppelingen: ongewijzigd.
- Geen fork; historische pre-flip totalen hoeven niet bewaard te blijven.

**Fixture-contracttests (nieuw):**

- Na mutatie: **121 aging, 0 random** faalwijzen.
- Geflipte FM's: `sigma_jaar` expliciet = 0,15×MTTF (1 decimaal).
- Metadata (`aanname_faalmodel`, `notes`) aging-logisch (niet identiek aan oude random-tekst).

Geen baseline-refresh in dit issue — verwachte breuk van `test_run_metrics_baseline` tot issue 03.

## Acceptance criteria

- [ ] Alle 58 voormalige random-FM's zijn `aging` met expliciete `sigma_jaar`.
- [ ] `aanname_faalmodel` en `notes` bijgewerkt voor geflipte FM's (G2).
- [ ] Fixture-contracttest: 121 aging / 0 random.
- [ ] Fixture-contracttest: sigma en metadata voldoen aan G2+G3-regels.
- [ ] `test_haarlem_fixture_has_no_synthetic_nmf_tst_tasks` blijft groen.
- [ ] Issue 01 tests blijven groen.

## Blocked by

- Issue 01 — `.scratch/rcm-desktop-slice42-lcc-cm-year-correctness/issues/01.md`
