# Issue 01 — LCC CM shape helper + synthetische vormtests (D1)

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice42-lcc-cm-year-correctness/PRD.md` (slice 42).

## What to build

**Test helper (pure, geen Qt):**

- Shape-characterization seam voor LCC correctief jaarprofiel, conceptueel:

```python
@dataclass(frozen=True)
class LCCCmYearShape:
    buckets: tuple[float, ...]
    cv: float
    max_mean_ratio: float
    aging_cm_share: float
    used_legacy: bool
    reconciles: bool

def characterize_cm_year_shape(project, fm_results, *, build=build_cor_eur_per_bucket) -> LCCCmYearShape: ...
```

- Encapsuleert: bucket-aggregatie, CV, max/mean, aging-CM-share (via faalwijze failure_type), DS-4b reconcile, `used_legacy`-flag uit `build_cor_eur_per_bucket`.

**Synthetische vormtests (D1):**

End-to-end via productketen: minimale inline `RCMProject.from_dict` fixtures → `run_analytical(parallel=False)` → `build_cm_eur_per_bucket` / `horizon_profile.cor_eur`.

| Scenario | Verwacht gedrag |
|----------|-----------------|
| Enkele **random** FM | CV(jaarlijkse CM) < 0,01; reconcile |
| Enkele **aging** FM (geen REV) | max/mean > 2,0; piekjaar ≈ MTTF (±1 bucket); reconcile |
| Horizon profile pad | `horizon_profile` aanwezig; geen legacy fallback (`used_legacy is False`) |

Geen wijziging aan Haarlem-fixture, `rcm_core`-algoritmes, adapter UI, of `CACHE_INPUTS_VERSION`.

## Acceptance criteria

- [ ] Shape helper module bestaat met unit tests voor CV, max/mean, aging-share op bekende bucket-arrays.
- [ ] Random-FM synthetische test: CV < 0,01 en DS-4b reconcile.
- [ ] Aging-FM synthetische test: max/mean > 2,0, piek rond MTTF (±1 bucket), reconcile.
- [ ] Test bewijst `used_legacy is False` en gebruik van `horizon_profile.cor_eur` op verse engine-run.
- [ ] Volledige relevante pytest groen **zonder** Haarlem-fixture mutatie.
- [ ] Geen wijzigingen aan `rcm_core.models` of editing schemas.

## Blocked by

None — can start immediately.
