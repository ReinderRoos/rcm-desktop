# Issue 27 — Vergelijkactie CM->PM met gedeeld projectsnapshot

**Status:** done  
**Type:** AFK

## Parent

- `../PRD.md` (slice 10 — scenariovergelijker CM vs PM)

## What to build

Voeg een expliciete gebruikersactie `Vergelijk CM vs PM` toe binnen de bestaande
single-project validate-flow. Deze actie voert twee scenario-runs sequentieel uit
op exact hetzelfde gematerialiseerde projectsnapshot.

De slice levert end-to-end:

- aparte vergelijkactie naast bestaande `Run --full`;
- sequentiële orchestratie CM gevolgd door PM;
- hergebruik van bestaande edit-error gating vóór run-start;
- één samengesteld scenario-runresultaat als input voor vervolgweergave.

Deze slice verandert het gedrag van `Run --full` niet.

## Acceptance criteria

- [x] De UI biedt een aparte actie `Vergelijk CM vs PM` zonder regressie op
      bestaande `Run --full`.
- [x] Beide scenario-runs worden sequentieel uitgevoerd op hetzelfde
      gematerialiseerde project.
- [x] De actie gebruikt dezelfde edit-error gating als bestaande run-flow.
- [x] Bij succesvolle dubbel-run is één samengesteld vergelijkingsresultaat
      beschikbaar voor de viewlaag.

## Blocked by

None - can start immediately

## Done notes

- Nieuwe compare-actieknop toegevoegd in `ValidateWindow`.
- Sequentiële CM->PM orchestration geïmplementeerd in adapter.
- Vergelijkactie hergebruikt bestaande edit-error gating via `materialize_for_run`.
- Bestaande `Run --full` gedrag ongewijzigd behouden.

