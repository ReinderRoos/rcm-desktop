# Issue 02 — PBS-gebonden uitrol: REV-taken + Correctief

**Triage:** ready-for-agent  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice87-input-grid-filter-performance/PRD.md` (slice 87).

## What to build

Breid de tracer uit naar de overige **PBS-gebonden** Input-views:

- **REV-taken**: scope via `fm_id` → faalwijze.`pbs_id` in het project.
- **Correctief**: directe `pbs_id` op de rij (zelfde entiteit als Faalwijzen,
  andere kolompreset).

Zoekbereik compleet: key_field ook bij verborgen kolom; display én ruwe
waarden; afgeleide PM-kolommen wanneer zichtbaar. View-wissel wist zoektekst.

## Acceptance criteria

- [ ] REV-taken en Correctief volgen actieve PBS-scope (subtree).
- [ ] REV-taken scope-resolve via `fm_id` → faalwijze.`pbs_id` werkt correct.
- [ ] Zoeken op REV-taken vindt `pm_id` ook wanneer kolom verborgen is.
- [ ] Zoeken matcht op displaytekst én ruwe waarden (bv. faaltype-code én label).
- [ ] Onbekende scope levert lege tabel op beide views.
- [ ] View-wissel wist zoektekst (geen carry-over van Faalwijzen-filter).

## Blocked by

- `.scratch/rcm-desktop-slice87-input-grid-filter-performance/issues/01.md`
