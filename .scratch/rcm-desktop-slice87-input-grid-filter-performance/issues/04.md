# Issue 04 — `rows()`-cache in EntityEditService

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice87-input-grid-filter-performance/PRD.md` (slice 87).

## What to build

Cache de gesorteerde `EntityRowView`-lijst in `EntityEditService.rows()`.
Invalideren bij alle mutatiepaden: `apply_change`, `apply_bulk_change`,
`discard_changes`, `reset`/`init`, `attach_editing_session`, `clear`.

Test via observeerbaar gedrag (inhoud klopt na mutatie); geen asserts op
private cache-velden.

## Acceptance criteria

- [ ] Twee opeenvolgende `rows()`-aanroepen leveren identieke inhoud.
- [ ] Na `apply_change` reflecteert `rows()` de wijziging.
- [ ] Na `discard_changes` terug naar originele inhoud.
- [ ] Na `reset`/`init`/`attach_editing_session`/`clear` cache correct
      ververst.
- [ ] `tests/test_editing_schemas_parity.py` blijft groen.

## Blocked by

None — can start immediately.
