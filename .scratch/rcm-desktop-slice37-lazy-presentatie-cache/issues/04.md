# Issue 04 — Lazy LCC warmup via render_index

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice37-lazy-presentatie-cache/PRD.md` (slice 37).

## What to build

LCC-presentatie **niet** in presentatie-cache worker. Bij modus LCC: render via `WorkspaceRenderIndex.get_or_build` + `build_lcc_planning_curve_reconciled` (bestaand pad). `_maybe_start_presentation_rebuild` alleen voor ontbrekend contribution-blok.

Pure helper `presentation_modus_needs_rebuild(project, path, modus)` voor modus-bewuste rebuild-beslissing.

## Acceptance criteria

- [ ] Presentation rebuild start niet voor LCC-only gap.
- [ ] Moduswissel naar LCC triggert render via render_index (bestaande `_render_lcc_for_run`).
- [ ] Unit test: `presentation_needs_rebuild` vs `presentation_modus_needs_rebuild("lcc")` gedrag.

## Blocked by

- Issue 02
