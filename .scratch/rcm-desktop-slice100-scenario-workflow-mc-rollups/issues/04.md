# Issue 04 — Scenario slot state + freeze lifecycle

**Type:** AFK  
**Triage:** `done`
**Outcome:** ScenarioWorkflowService wired in window; CompareMcSlotState consolidated; invalidation hooks; tests/test_slice100_scenario_workflow.py + test_slice100_scenario_invalidation.py.

## Parent

PRD: `.scratch/rcm-desktop-slice100-scenario-workflow-mc-rollups/PRD.md`

## What to build

Unified **scenario 1 / scenario 2** slot model (intern mapbaar op bestaande A/B keys) met **run-modus** en gemengde resultaattypes.

- Breid `CompareSlotSnapshot` uit: `run_mode`, analytische `RunResult` **of** MC snapshot / P50 rollup reference, bevroren overlay/planning, scope, label metadata.
- Consolideer `CompareMcSlotState` in één slot state (geen parallelle A/B MC store).
- **ScenarioWorkflowService** (naam TBD): `freeze_live_as_scenario_1`, `put_scenario_2`, `clear_all`, `has`, `both_filled`, leesbare labels (analytisch vs MC N=…).
- **Freeze alleen op Extra scenario** — niet automatisch na eerste Start analyse.
- Invalidatie: padwijziging, validate/import-success, leeg project → slots hard clear (extend slice 56).
- Mislukte run scenario 2 laat scenario 1 intact.

Unit tests voor transities; nog geen volledige Extra scenario UI (issue 05).

## Acceptance criteria

- [ ] Eén slot state type ondersteunt analytisch + MC snapshots per scenario
- [ ] `freeze_live_as_scenario_1` bewaart run-modus + resultaat + overlay/scope op freeze-moment
- [ ] Start analyse (simulated) wijzigt gevulde scenario-slots niet
- [ ] `both_filled` false tot scenario 2 succesvol; clear_all op invalidatie-events
- [ ] Slot-labels onderscheiden analytisch vs MC (minimaal in API/string builder)
- [ ] Unit tests: `tests/test_compare_slot_state.py` patronen + slice100 scenario workflow tests

## Blocked by

Issue 02 (`.scratch/rcm-desktop-slice100-scenario-workflow-mc-rollups/issues/02.md`)

## User stories covered

17, 18, 19, 20, 37, 38, 39
