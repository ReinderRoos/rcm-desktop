# Issue 02 — MC P50 rollup + live Top 10/LCC routing

**Type:** AFK  
**Triage:** `done`
**Outcome:** MC P50 rollup + live Top10/LCC routing via simulation_workspace_service; tests/test_slice100_mc_p50_rollups.py.

## Parent

PRD: `.scratch/rcm-desktop-slice100-scenario-workflow-mc-rollups/PRD.md`

## What to build

End-to-end adapter-pad: na geslaagde **Monte Carlo-run** vullen **Top 10** en **LCC** vanuit MC **P50** per faalwijze — zelfde presentatiepipeline als analytisch.

- Introduceer MC → synthetisch `RunResult`-vormige rollup (P50 voor failures, downtime, kosten per FM; PBS/Top10/LCC aggregatie hergebruikt).
- Wijzig run-source resolutie: live view leest analytische slot bij Run-modus analytisch; **MC P50 rollup** bij Run-modus MC + voltooide MC-run.
- FM-resultaten in MC-modus blijven **banden** (P50 kolom + P10/P90 tooltip) — geen regressie slice 98.
- MC-only sessie (geen eerdere analytische run) moet Top 10/LCC kunnen vullen.
- Dual namespace bij mode-switch zonder rerun blijft intact.

Tests op adapter-seam (rollup factory, `simulation_workspace_service`, orchestrator/view service: non-empty Top 10/LCC plans na MC).

## Acceptance criteria

- [x] P50 rollup levert `RunResult`-compatibele rows/metrics; seeded parity P50 ≈ analytisch binnen bestaande tolerantie
- [x] `top10_lcc_reads_*` (of opvolger) routeert MC-modus naar P50 rollup, niet naar lege analytische slot
- [x] Orchestrator/view service bouwt Top 10 en LCC views na MC-only run (unit tests)
- [x] FM-detail MC-modus ongewijzigd banden-gedrag; slice 98 MC tests blijven groen
- [x] Presentation-scale / NB-filter consistent toegepast op rollup (zelfde semantiek als analytische FM rows)

## Blocked by

Issue 01 (`.scratch/rcm-desktop-slice100-scenario-workflow-mc-rollups/issues/01.md`)

## User stories covered

1, 2, 3, 4, 5, 6, 44
