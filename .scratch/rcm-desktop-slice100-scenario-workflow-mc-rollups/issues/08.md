# Issue 08 — Vergelijk FM-resultaten split (metric columns)

**Type:** AFK  
**Triage:** `done`
**Outcome:** FM compare split (fm_compare render plan + panel); metric-gestuurde kolommen via bestaande FM models; tests/test_slice100_compare_mixed_fm.py.

## Parent

PRD: `.scratch/rcm-desktop-slice100-scenario-workflow-mc-rollups/PRD.md`

## What to build

Breid **Vergelijk scenario's** uit naar **FM-resultaten**: horizontale split (scenario 1 | scenario 2).

- Orchestrator compare branch voor FM-detail (vandaag uitgesloten in ADR-0007 v1).
- **Metric-gestuurde kolommen:** NB → downtime; Faalmomenten → failures; Kosten → cost (volgt globale metric toggle).
- MC-scenario in compare: **P50** waarden in kolommen; P10/P90 alleen in tooltip (slice 98 formaat).
- Gemengde run-modus: punt vs P50 per FM-rij.
- Gedeelde sortering/filtering over beide FM-panelen waar van toepassing.
- Compare toggle uit: bestaand single-run FM (analytisch punt of MC banden).

Smoke: FM view split zichtbaar wanneer Vergelijk scenario's aan + both_filled.

## Acceptance criteria

- [ ] FM compare split render plan wanneer compare_mode + both_filled + FM view actief
- [ ] Kolommen wisselen met metric toggle (kosten / faalmomenten / NB-downtime)
- [ ] MC slot toont P50 + band tooltip; analytical slot toont puntwaarden
- [ ] Mixed S1/S2 run-modus compare werkt in FM split
- [ ] Single-run FM zonder compare ongewijzigd (analytisch + MC banden)
- [ ] Orchestrator + window smoke tests voor FM compare pad

## Blocked by

Issue 07 (`.scratch/rcm-desktop-slice100-scenario-workflow-mc-rollups/issues/07.md`)

## User stories covered

25, 26
