# Issue 05 — Jaarlijkse effectklassen (M8c)

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice27-nmf-jaartoerekening/PRD.md` (slice 27).

## What to build

Breid `FMResult` uit met **jaarlijkse effectbijdragen** voor LCC/NB-presentatie (M8c),
los van het NMF-profiel in issue 03 maar in hetzelfde horizonmodel.

**Toerekening per kalenderjaar h:**
`effect_bijdragen[h, k] ← faalmomenten[h] × link.fractie × duur` (via bestaande
FM/PM-effectlinks), consistent met DS-1d en slice 24 O-4.

**Motor:** vul jaarlijkse effectreeksen naast `FMHorizonProfile` (zelfde horizonlengte);
reconcilieer som over jaren met bestaande lifecycle-effecttotalen op `FMResult`.

**Presentatie:** LCC- en NB-adapters sommeren jaarlijkse effecten voor de relevante
grafieken/modussen die effectklassen per jaar tonen.

**Bijdragen-modus:** blijft **lifecycle-totaal** uit `FMResult` — geen UI-herschrijving
van contribution charts in deze issue.

## Acceptance criteria

- [x] `FMResult` bevat jaarlijkse effectdata (vorm vastgelegd in issue, bv. dict per
      effectklasse-id of parallelle lijsten); cache round-trip.
- [x] Test: som jaarlijkse effect EUR (of equivalent) ≈ lifecycle-effecttotaal per FM
      binnen ε.
- [x] Test: aging-faal met piek faalmomenten in jaar h → effectbijdrage concentratie in
      jaar h (niet uniform lifecycle-smear).
- [x] LCC/NB adapter-tests tonen jaarlijkse effectcomponent in output wanneer modus dat
      vereist.
- [x] Bijdragen-chart service **ongewijzigd** qua lifecycle-contract (regressietest).
- [x] `CACHE_INPUTS_VERSION` verhoogd indien schema/hash wijzigt.
- [x] Geen scrub-list features; geen Qt in kern.

## Blocked by

- Issue 03 (horizonlengte en jaartoerekening-patroon op `FMResult`).
