# Issue 01 — FM-verificatie documenteren in CONTEXT.md

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice34-fm-verificatie-werkruimte/PRD.md` (slice 34).

## What to build

Leg in `CONTEXT.md` vast **waar en hoe** een analist één faalwijze (FM) controleert — zonder code-wijzigingen.

End-to-end (documentatie):

- Nieuwe sectie *FM-verificatie & spot-check* met **drie lagen**:
  1. **pytest / CLI** — strengste, reproduceerbare verificatie (fixtures, motor-`FMResult`, reconcile-tests).
  2. **Resultatenwerkruimte, modus FM-detail** — interactieve plek na slice 34 (inspector; issues 02–04).
  3. **Legacy ValidateWindow** (`--legacy-validate`) — projectcockpit (bewerken, run, LTAP/PM what-if); **niet** aanbevelen voor jaar-voor-jaar FM-spot-check.
- Korte disclaimer: jaarreeks-inspectie in UI komt via slice 34, niet via ValidateWindow-layout (slice 8 deferred).
- Verwijs naar bestaande termen: **FM-invoerhash**, **horizon_profile**, **resultatenwerkruimte**.

**Niet in deze slice:** `fm_verification_service`, UI-inspector, slice-8 layout-code.

## Acceptance criteria

- [x] `CONTEXT.md` bevat sectie *FM-verificatie & spot-check* met drie lagen zoals hierboven.
- [x] Legacy ValidateWindow heeft expliciete disclaimer (geen FM-laboratorium).
- [x] FM-detail in resultatenwerkruimte wordt genoemd als interactief pad (na implementatie issues 02–04).
- [x] Geen wijziging aan `rcm_core` of adapter/UI-code.

## Blocked by

None — can start immediately.
