# Issue 04 — FM-inspector: jaartabel + reconcile

**Triage:** done  
**Type:** AFK

## Parent

PRD: `.scratch/rcm-desktop-slice34-fm-verificatie-werkruimte/PRD.md` (slice 34).

## What to build

Breid het FM-inspectorpaneel (issue 03) uit met **jaar-voor-jaar verificatie** en **reconcile-status** — read-only, alleen in FM-detail.

End-to-end:

- Nieuwe Qt-tabel **`fm_verification_year_table_model`**: kolommen kalenderjaar, faalmomenten (proxy), correctief EUR, downtime uur, verborgen NB uur; formatting/labels via `messages`.
- Inspector toont **reconcile-indicator** (OK / waarschuwing) en korte `reconcile_notes` uit `FMVerificationView`.
- Bij **`profile_missing`**: duidelijke melding dat alleen lifecycle-totalen betrouwbaar zijn; geen jaartabel die stilletjes lege OK suggereert.
- Jaartabel **scrollbaar** bij smal venster.
- Optioneel korte tooltip/disclaimer: jaar-faalmomenten zijn presentatie-proxy (zelfde mentaliteit als Top 10/LCC).
- Unit-tests op year table model (kolommen, display roles).
- pytest-qt: FM met `horizon_profile` in fixture-run → minstens één kalenderjaar in jaartabel + reconcile-status zichtbaar.

**Niet in deze slice:** bewerken van FM-invoer, LTAP what-if, scenario-vergelijking, motorwijzigingen.

## Acceptance criteria

- [x] Geselecteerde FM met `horizon_profile` toont jaartabel met kalenderjaren en bucket-kolommen.
- [x] Reconcile OK/waarschuwing en notes zijn zichtbaar in inspector.
- [x] FM zonder `horizon_profile` toont profile-missing melding; geen stille reconcile-OK.
- [x] Kalenderjaar K in inspector komt overeen met mapping in LCC/Tijdsplot (zelfde `modeljaar`-basis).
- [x] Unit-tests `fm_verification_year_table_model` slagen.
- [x] pytest-qt uitbreiding: jaartabel + reconcile zichtbaar na selectie (waar fixture profile heeft).
- [x] Inspector blijft read-only; alleen modus FM-detail.

## Blocked by

- Issue 02 (`fm_verification_service`)
- Issue 03 (FM-inspector layout + selectie)
